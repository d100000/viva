# 功能优化清单与产品功能清单

> 依据：① 火山引擎豆包流式语音识别 API 的**全部可用参数**（[02 章](02-豆包流式ASR接入.md)）
> ② 市面主流语音输入产品的功能拆解（[05 章](05-产品设计方案.md)）
> ③ 当前 `app/` 版本 v0.2.0 的实际实现
>
> 每一项都标了 **价值 / 成本 / 依据**。价值按「能不能让用户留下来」评，不是按「听起来酷不酷」。

---

## 0. 当前基线：v0.2.0 已实现

| 模块 | 已有 |
|---|---|
| 识别 | 豆包 2.0 流式（`bigmodel_async`）、逐句 definite 上屏、自动标点 / ITN / 语义顺滑 |
| 热词 | `corpus.context` 直传，可视化词库管理（标签流 + 批量编辑） |
| 交互 | 自定义热键（单修饰键 / 组合键，支持录制）、按住说话、Esc 取消、悬浮 HUD 双态渲染 |
| 上屏 | 剪贴板 + ⌘V / CGEvent 逐字，Secure Input 检测与剪贴板兜底，前台 App 切换保护 |
| 音频 | 引擎常驻预热 + 400ms 环形预缓冲（首字不丢） |
| 数据 | 历史记录持久化、按日分组、搜索、Markdown/CSV 导出 |
| 统计 | 次数 / 时长 / 字数 / 语速 / 节省时间 / 连续天数 / App 分布 / 12 周热力图 / 实时费用 |
| 工程 | 配置容错解码、CGEventTap 自愈、协议自检模式（`--selftest`） |

---

## 1. 豆包 API 还没用上的能力

这是**最高性价比的一块**——服务端已经做好了，客户端只要传参数。

### 1.1 立刻该做（改几行参数就有明显收益）

| 能力 | 参数 | 价值 | 成本 | 说明 |
|---|---|---|---|---|
| **替换词表** | `corpus.correct_table_name/id` | ⭐⭐⭐ | 低 | 强制 A→B。热词只能「提高概率」，替换词是**确定性纠正**。「克劳德」→「Claude」这类固定错误必须靠它 |
| **自学习平台热词表** | `corpus.boosting_table_name/id` | ⭐⭐⭐ | 低 | 直传 `context` 只有约 100 tokens，词表能放**几千个词**。技术术语库根本塞不进直传 |
| **繁体输出** | `output_zh_variant` = `traditional`/`tw`/`hk` | ⭐⭐ | 极低 | 港台用户刚需，一个下拉框的事 |
| **敏感词过滤** | `sensitive_words_filter` | ⭐ | 低 | 企业场景合规。支持过滤/替换为 * /自定义词 |
| **首字加速** | `enable_accelerate_text` + `accelerate_score`(0~20) | ⭐⭐ | 极低 | 做成「极速模式」开关，用准确率换首字延迟。**要在 UI 上讲清代价** |
| **增量返回** | `result_type: "single"` | ⭐ | 低 | 目前用 `full` + 游标，改增量能减少客户端处理量。收益小，但顺手 |

### 1.2 值得做（有明确用户场景）

| 能力 | 参数 | 价值 | 成本 | 说明 |
|---|---|---|---|---|
| ⭐ **对话上下文** | `corpus.context` + `context_type: "dialog_ctx"` | ⭐⭐⭐ | 中 | 限 800 tokens / 20 轮。**把前台 App 名、窗口标题、最近几条识别结果传进去，识别会显著更准**。这是「可编程热词」的进阶版 |
| ⭐⭐ **视觉上下文** | `context_data` 里传 `image_url`（豆包 2.0 独有，≤500KB） | ⭐⭐⭐ | 高 | **截当前窗口给模型看，帮它理解你在说什么**。在 IDE 里口述时，模型能看到你的代码 → 变量名、函数名识别率大幅提升。<br>⚠️ **隐私红线**：截屏上传是本品类公认的信任雷区，必须默认关闭 + 显式 opt-in + 明确告知 |
| **多语种指定** | `audio.language`（25 语种） | ⭐⭐ | 中 | ⚠️ **仅 `bigmodel_nostream` 支持**，双向流式不支持。要做多语言得走「松手整段」模式，是个架构分叉 |
| **语种自动识别** | `enable_auto_lang`（仅 nostream） | ⭐⭐ | 中 | 同上 |
| **语速 / 音量** | `show_speech_rate` / `show_volume` | ⭐ | 低 | 统计页可以展示真实语速（现在是自己算的）。⚠️ 开启会强制启用 800ms VAD 分句，**会覆盖你的 `end_window_size` 设定**，慎用 |

### 1.3 场景化，暂不做

| 能力 | 参数 | 判断 |
|---|---|---|
| 说话人分离 | `enable_speaker_info` + `ssd_version: "200"` | 会议转写才有用，语音**输入**场景一个人说话，无价值 |
| 情绪 / 性别检测 | `enable_emotion_detection` / `enable_gender_detection` | 输入法场景用不上；且会强制开 VAD 分句 |
| 语种检测标签 | `enable_lid` | 同上 |
| POI / 音乐 function call | `enable_poi_fc` / `enable_music_fc` | 地图和音乐领域专用，与本产品定位无关 |
| Opus 编码 | `format: ogg` + `codec: opus` | 省 90% 带宽，但桌面端不缺带宽，增加编码延迟和依赖。**移动端才做** |

### 1.4 换端点/换 API 才能做的

| 能力 | 需要 | 价值 | 说明 |
|---|---|---|---|
| **高准确率模式** | `bigmodel_nostream` 端点 | ⭐⭐ | 「松手整段」模式下改用它，准确率更高。可以做成 `commitOnlyAtEnd` 开启时自动切端点 |
| **长音频转写** | 录音文件识别 API（`volc.bigasr.auc_turbo`） | ⭐⭐ | 拖一个会议录音进来转写。0.8 元/小时，比流式便宜 |
| **边说边翻译** | 同声传译 2.0 API | ⭐⭐ | 说中文出英文。海外产品普遍有，中文侧空白 |

---

## 2. 竞品有、我们没有的功能

按「能力分层」组织，L1→L4 逐层加深。

### L1 · 基础后处理 —— 豆包 ASR 已内建，我们已有 ✅

标点、ITN、去口水词。**这层已经不是差异化了**，豆包输入法免费送。

### L2 · 场景化配置 —— 竞品普遍有，我们缺 ❌

| 功能 | 谁在做 | 价值 | 成本 |
|---|---|---|---|
| ⭐⭐⭐ **按 App 自动切换配置**（Power Mode / Smart Modes） | VoiceInk、superwhisper、Typeless、Wispr | ⭐⭐⭐ | 中 |
| **风格预设**（个人消息 / 工作消息 / 邮件 / 代码） | Wispr Style、superwhisper Modes | ⭐⭐⭐ | 中 |
| **片段 / 缩写展开**（Snippets） | Wispr Snippets | ⭐⭐ | 低 |
| **智能排版**（口述清单自动分行、代码块识别） | Typeless | ⭐⭐ | 中 |

> **这一层是当前最大的缺口。** 具体设想：
> - 在 Cursor / VS Code / 终端 → 加载技术热词、关闭润色、保持英文标识符不转拼音、用 CGEvent 逐字（避免污染剪贴板）
> - 在飞书 / 微信 → 口语转书面语、剪贴板上屏
> - 在备忘录 / Obsidian → 轻度润色、自动分段

### L3 · LLM 深加工 —— 竞品人手一份，我们完全没有 ❌

| 功能 | 谁在做 | 价值 | 成本 |
|---|---|---|---|
| ⭐⭐⭐ **口语 → 书面语润色** | 全部竞品 | ⭐⭐⭐ | 中 |
| ⭐⭐⭐ **选中文本 + 语音指令就地改写** | Aqua Edit Mode、Wispr Command Mode | ⭐⭐⭐ | 中 |
| **自定义 prompt 模板**（`{text}`/`{selected}`/`{clipboard}` 变量） | type4me、superwhisper | ⭐⭐ | 低 |
| **多 LLM 供应商可选** | superwhisper（GPT-5 / Claude / Gemini…）、type4me（13 家） | ⭐⭐ | 低 |
| **翻译输出**（说中文出英文） | Wispr、Aqua | ⭐⭐ | 低 |

> ⚠️ **实现要点（[05 章](05-产品设计方案.md) §5.3 已论证）**：
> **必须两段式** —— 流式期间只出原始 ASR（保实时感），definite 或整段结束后再用 LLM 原地精修。
> **绝不能在流式过程中反复调 LLM**，否则屏幕上的字会反复跳变重写。
>
> 「选中文本 + 语音指令改写」（L3 核心）在**中文侧基本空白**，且直击 V2EX 排名第三的痛点「说错一点就得重来」。**这是最值得做的一个。**

### L4 · Agent 化 —— 前沿，闪电说在做

| 功能 | 谁在做 | 判断 |
|---|---|---|
| 长按唤起 AI 助手执行技能 | 闪电说 | 需要完整的技能体系，超出「输入工具」定位 |
| 结合屏幕 / 个人记忆 / 知识库理解语境 | 闪电说 | 隐私成本高 |
| Agent Mode | Spokenly | — |

> **建议不追。** 这一层需要的投入远超输入工具本身，而且闪电说已经占位。

### 交互层缺口

| 功能 | 谁在做 | 价值 | 成本 |
|---|---|---|---|
| **免提常开模式**（VAD 自动断句） | 豆包输入法、微信输入法（Fn+空格） | ⭐⭐ | 中 |
| **个人改词记忆**（改一次，长期生效） | 豆包输入法 | ⭐⭐⭐ | 中 |
| **Flow Bar 浮动控制条**（可拖动、右键菜单） | Wispr | ⭐ | 中 |
| **粘贴上一段**全局快捷键 | Wispr | ⭐⭐ | 极低 |
| **文件转写**（拖入音频） | superwhisper、MacWhisper | ⭐⭐ | 中 |
| **本地离线后备** | VoiceInk、闪电说、Spokenly、type4me | ⭐⭐ | 高 |

> ⚠️ **免提常开要慎重**：按小时计费 + 常开 = 挂机 8 小时 8 元/天。必须配会话级 VAD 门控。
> 而**「个人改词记忆」性价比极高**：用户在历史记录里改一次「Cloth→Claude」，自动写进替换词表，下次就对了。这是把 L2/L3 能力用最低成本兑现的做法。

---

## 3. 完整功能清单（按优先级）

### P0 · 现在就该补（1~2 周）

| # | 功能 | 价值 | 成本 | 依据 |
|---|---|---|---|---|
| 1 | **替换词表**（`correct_table` + 本地正则双通道） | ⭐⭐⭐ | 低 | 热词只提概率，替换才确定 |
| 2 | **个人改词记忆** —— 历史记录里改一次，自动进替换词表 | ⭐⭐⭐ | 中 | 对标豆包输入法 |
| 3 | **粘贴上一段**全局快捷键 | ⭐⭐ | 极低 | Secure Input / 终端场景的唯一兜底 |
| 4 | **App 兼容性矩阵**（按 bundleId 记住哪种上屏方式可用） | ⭐⭐⭐ | 中 | [06 章 M0.5](06-实现路径与里程碑.md) 的必然产物 |
| 5 | 首字延迟指标修正（从人声起点算） | ⭐ | 极低 | ✅ 已完成 |
| 6 | **繁体输出**开关 | ⭐⭐ | 极低 | 一个下拉框 |

### P1 · 让人愿意留下来（2~4 周）

| # | 功能 | 价值 | 成本 |
|---|---|---|---|
| 7 | ⭐ **按 App 自动切换配置（Profile）** | ⭐⭐⭐ | 中 |
| 8 | ⭐ **两段式 LLM 润色**（definite 后精修，可选模型 + 自定义 prompt） | ⭐⭐⭐ | 中 |
| 9 | **对话上下文注入**（前台 App / 窗口标题 / 最近识别结果 → `dialog_ctx`） | ⭐⭐⭐ | 中 |
| 10 | **可编程热词**（扫描当前 Git 仓库的标识符自动生成热词） | ⭐⭐⭐ | 中 |
| 11 | **自学习平台热词表**接入（突破 100 tokens 限制） | ⭐⭐ | 低 |
| 12 | **点按开关模式**（长文口述不用一直按着） | ⭐⭐ | 低 |
| 13 | **快嘴 / 思考模式**一键切换（含 `end_window_size` + 端点切换） | ⭐⭐ | 低 |
| 14 | 统计页增强：字数里程碑、与打字对比的可视化、导出报告 | ⭐ | 低 |

### P2 · 差异化壁垒（1~2 月）

| # | 功能 | 价值 | 成本 |
|---|---|---|---|
| 15 | ⭐⭐ **选中文本 + 语音指令就地改写**（中文侧空白） | ⭐⭐⭐ | 中 |
| 16 | **翻译输出**（说中文出英文 / 同声传译 API） | ⭐⭐ | 中 |
| 17 | **文件转写**（拖入音频，走录音文件识别 API，0.8 元/小时） | ⭐⭐ | 中 |
| 18 | **免提常开模式** + 会话级 VAD 门控 | ⭐⭐ | 中 |
| 19 | ⚠️ **视觉上下文**（截当前窗口辅助识别，默认关 + 显式 opt-in） | ⭐⭐⭐ | 高 |
| 20 | **本地离线后备**（FunASR / Whisper） | ⭐⭐ | 高 |

### P3 · 形态突破

| # | 功能 | 价值 | 成本 | 风险 |
|---|---|---|---|---|
| 21 | **IMK 输入法形态**（真·逐字上屏，marked text 预编辑） | ⭐⭐⭐ | 极高 | 麦克风权限 + 双进程 + 用户要放弃拼音方案，见 [04 章 §6](04-技术实现方案.md) |
| 22 | **Windows 版** | ⭐⭐ | 高 | 豆包输入法 Windows 版尚未发布，有时间窗口 |
| 23 | 移动端 | ⭐ | 极高 | 输入法形态无法回避 |

---

## 4. 优化清单（非功能）

### 4.1 性能与延迟

| 项 | 现状 | 优化 | 收益 |
|---|---|---|---|
| 首字延迟 | ~600ms（自检口径） | `enable_accelerate_text` 做成「极速模式」 | -100~200ms，代价是准确率 |
| WebSocket 建连 | 每次会话新建 | 短窗口预热（≤8s）+ 失败重试 | -100~300ms。⚠️ 会撞 `45000081`，需谨慎 |
| 音频回调 | 实时线程里做 Data 拼接 + `Task { @MainActor }` | 换成无锁环形缓冲 + 批量投递 | 降低音频线程抖动风险 |
| LevelMeter | 每个音频回调触发 SwiftUI 更新 | 降频到 ~20Hz | 降 CPU |
| 历史记录 | 全量 JSON，每次统计遍历全表 | 记录数大时改增量统计 / SQLite | 上千条后才有必要 |

### 4.2 体验

| 项 | 问题 | 优化 |
|---|---|---|
| ⭐ **权限授权后要重启** | 用户在系统设置里打开开关，App 仍显示未授权，**极易被当成 bug**（你已经踩到） | 检测到授权状态变化时，弹「已检测到授权，需要重启才能生效」+ 一键重启按钮 |
| ⭐ **重新编译使 TCC 失效** | ad-hoc 签名每次变，热键静默失灵 | 界面上做「热键健康」指示；文档写清「移除再重新添加」 |
| 菜单栏图标太隐蔽 | 用户以为「软件没打开」（你已经踩到） | 首次启动直接开主界面 ✅ 已做；再加首启动引导页 |
| HUD 位置 | 拿不到光标位置时回落屏幕底部 | 支持手动拖动并记住位置（对标 Wispr Flow Bar） |
| 错误文案 | 部分仍是技术表述 | 全部错误码翻译成「怎么办」 |
| 无撤销 | 上屏后想撤回只能自己删 | 「撤销上一次上屏」快捷键 |

### 4.3 工程质量

| 项 | 状态 |
|---|---|
| 配置容错解码 | ✅ 已做（否则加字段就会丢 API Key） |
| CGEventTap 自愈 | ✅ 已做 |
| 协议自检模式 | ✅ 已做 |
| 单元测试 | ❌ 缺 —— 协议编解码、统计计算、diff 算法都应该有 |
| 崩溃上报 | ❌ 缺，且**日志必须脱敏，绝不能带识别文本** |
| 自动更新 | ❌ 缺（Sparkle） |
| 签名与公证 | ❌ 目前 ad-hoc。分发必须 Developer ID + 公证（辅助功能与沙盒不兼容，上不了 App Store） |

---

## 5. 建议的下一步顺序

按「每一步都能立刻感知到收益」排：

```
第 1 步（2~3 天）  替换词表 + 个人改词记忆 + 粘贴上一段
                   → 立刻解决「Claude→Cloth」这类最高频抱怨

第 2 步（3~5 天）  按 App 自动切换配置（Profile）
                   → 一次投入，长期受益，且是竞品标配

第 3 步（3~5 天）  两段式 LLM 润色 + 自定义 prompt
                   → 补上唯一还缺的「标配能力」

第 4 步（1 周）    可编程热词（Git 仓库感知）+ 对话上下文注入
                   → 这才是真正拉开差距的地方

第 5 步（1~2 周）  选中文本 + 语音指令就地改写
                   → 中文侧空白，直击「说错就得重来」的痛点
```

**前四步做完，产品在功能完整度上就追平了 type4me / VoiceInk 这一档，同时在「热词可编程 + 首字不丢 + 成本透明」三点上领先。第五步是真正的差异化。**

> ⚠️ 但要清醒：[05 章](05-产品设计方案.md)的结论没有变 —— **豆包输入法免费、同源模型、已实现逐字流式上屏**。
> 功能追平不等于赢。真正的立足点仍然是那四条结构性差异：**不占输入法槽、首字不丢、热词可编程、隐私自持**。
> 补功能的同时，不要把这四条稀释掉。
