# 实现路径与里程碑

> 假设：1 人全职 / 或 1 人 + Claude Code 协作。时间是「有效工作日」。

---

## 总览

```
M0 协议验证   M0.5 注入门槛   M1 CLI 打通    M2 MVP 上线    M3 好用      M4 差异化
  1-2 天        3 天 ⛔        3-4 天         8-10 天       5-7 天       2-3 周
    │             │              │              │             │            │
  Python      go/no-go        Python        Swift App      打磨       IMK 输入法
  跑通协议     5 App × 3 注入   端到端        菜单栏+HUD      热词      语音指令编辑
  实测延迟     方式冒烟测试     麦克风→上屏    注入链+权限     两段式润色  场景 Profile
    │             │              │              │             │            │
    ▼             ▼              ▼              ▼             ▼            ▼
 "服务器      "这条路           "命令行        "自己每天      "别人愿意    "别人愿意
  能出字"      走得通"          能上屏"        都在用"        用"         付钱"
```

**两个关键原则**：
1. **每个里程碑都必须产出一个「能用的东西」**，而不是「一个模块」。
2. ⛔ **M0.5 是硬门槛** —— 不通过就不要往下走，回头重新评估 IMK 路线。

---

## M0 · 协议验证（1~2 天）

> **目标**：证明「豆包流式 ASR 能按预期工作」，把最大的外部不确定性先消灭。

### 任务

1. 火山引擎控制台开通**豆包流式语音识别模型 2.0**，拿 App Key / Access Token
2. 下载官方 `sauc_python.zip` Demo，跑通
3. 用 [02 章](02-豆包流式ASR接入.md) §9 的代码，改造成**读本地 wav 文件模拟推流**（先不碰麦克风）
4. 实测并记录：
   - [ ] 首字返回延迟（从发第一包到收到第一个字）
   - [ ] `definite` 在什么时机出现，`end_window_size` 从 800 调到 500/600 的效果差异
   - [ ] `enable_nonstream` 开/关的准确率与延迟差异
   - [ ] `result_type` = `full` vs `single` 的返回结构差异
   - [ ] 热词 `corpus.context` 直传是否生效（用一个生僻专有名词测）

5. ⭐ **验证 2.0 的 Resource ID**（`volc.seedasr.sauc.duration`）是否支持 `bigmodel_async` / `show_utterances` / `definite` / 热词直传 —— **所有开源实现用的都是 1.0 的 `bigasr`，2.0 无人验证过**
6. ⭐ **实测四段延迟**（这是全项目唯一的验收标准来源）：
   - 热键按下 → 首包音频发出
   - 首包 → 首个 partial 返回
   - partial → definite 到达
   - definite → 文本落进目标 App

### 交付物

- `spike-python/sauc_client.py` —— 一个**能打印每帧原始 header 字节**的抓包型客户端
- 一份**参数实测记录**（延迟/准确率对比表）→ 直接决定 M2 的默认配置

### 出口标准

> 播放一段 30 秒中文录音，控制台能**边播边逐句打印**识别结果，首字延迟 < 1 秒，且四段延迟数字已记录在案。

### ⚠️ 常见卡点（详见 [02 章 §10](02-豆包流式ASR接入.md)）

- `45000001` → 二进制协议拼错。**注意有两条都能跑通的路线（无序号 / 正序号），首包与末包必须一致，混用会解析错位**
- 拿不到 `definite` → 忘了 `show_utterances: true`
- 建连就断 → Header 名字写错，或 Resource ID 与开通的产品不匹配
- **收到 `definite:true` 就关连接 → 会丢掉用户后续说的全部内容**（`openless` 实测丢了 9 秒）。流结束只判帧头 flags
- 错误帧解析崩溃 → 官方文档格式与 `typeflux`/`type4me` 的实现不一致，**先 dump 原始字节再定型**

---

## M0.5 · 注入可行性冒烟测试（3 天）⛔ 硬门槛

> **目标**：在写任何产品代码之前，验证「非 IME 路线的产品前提是否成立」。
> 这是全项目**最大的单点风险** —— 协议是最简单的部分，注入才是。

### 任务

用最粗糙的脚本（Python/Swift 均可，不要工程化）跑完这张矩阵：

| | 备忘录 | Chrome 输入框 | VS Code | 微信 | iTerm2 |
|---|---|---|---|---|---|
| AX 直写 | | | | | |
| 剪贴板 + ⌘V | | | | | |
| CGEvent unicode | | | | | |

每格记录：成功率、失败模式、耗时。
额外测：在 iTerm2 里**开启 Secure Keyboard Entry** 后的行为。

### 出口标准（go/no-go）

> ✅ **通过**：剪贴板路径在 5 个 App 里至少 4 个成功 → 继续 M1
> ⛔ **不通过**：剪贴板路径 2 个及以上失败 → **停下**，回到 [04 章 §6](04-技术实现方案.md) 重新评估 IMK 路线

### 为什么这 3 天不能省

- AX 写入会**返回 success 但 UI 没变**，不实测根本发现不了
- Electron 的 AX 选区错乱是 **Closed as not planned**（官方永不修）
- Secure Input 下事件 tap 与合成按键**双双失效且无错误返回**
- 这三件事任何一件在 M2 才发现，都意味着架构返工

---

## M1 · CLI 端到端打通（3~4 天）

> **目标**：麦克风 → 识别 → 上屏，全链路跑通。**丑但能用。**

### 任务

1. Python 接麦克风（`sounddevice`），48k → 16k 重采样，200 ms 分包
2. 接 Silero VAD，静音不发包
3. 全局热键（Python 在 macOS 上支持很差，可先用「回车开始/结束」的土办法）
4. 最简上屏：`pbcopy` + AppleScript 模拟 ⌘V
5. 打印实时 partial 到终端

### 交付物

- `spike-python/main.py` —— 能在终端里说话，文字出现在另一个 App 里

### 出口标准

> 在终端运行脚本，对着麦克风说「今天下午三点开会」，备忘录里出现「今天下午 3 点开会。」

### ⚠️ 注意

**M1 是一次性的验证代码，不要在上面继续盖楼。** 它的唯一价值是证明链路可行、并让你实测到真实延迟体感。M2 直接用 Swift 重写。

---

## M2 · macOS MVP（8~10 天）⭐ 最关键

> **目标**：做出一个**你自己每天都在用**的菜单栏 App。

### 任务拆解

| # | 任务 | 天 | 说明 |
|---|---|---|---|
| 1 | Xcode 工程 + `VoiceCore` Swift Package 骨架 | 0.5 | 见 [04 章](04-技术实现方案.md) §10 |
| 2 | `AudioCapture`：AVAudioEngine + 重采样 + ⭐**环形预缓冲** | 1.5 | 回溯 300~500 ms，**这是差异化支点，不是可选项** |
| 3 | `DoubaoStreamingASR`：Swift 版协议实现 | 2 | 照搬 M0 定型的那一条路线，不做兼容分支 |
| 4 | `TextStabilizer` + ⭐`TextSink` 协议抽象 | 0.5 | 见下方「为 v2 留接口」 |
| 5 | `HotkeyManager`：CGEventTap 监听**右 ⌘** | 1.5 | ⚠️ **不要用 Fn**（微信/豆包已抢占）；必须处理 tap 静默禁用 |
| 6 | `InjectionChain`：三级策略链 + Secure Input 检测 | 2 | 按 M0.5 的实测结果配置 bundleId 策略表 |
| 7 | 悬浮 HUD：波形 + 双态文字 + 跟随光标 | 1.5 | `NSPanel` 不抢焦点；用 AX 只读 `kAXSelectedTextRange` 定位 |
| 8 | 菜单栏 + 设置窗（Key/热键/设备）+ 用量统计 | 1 | Key 存 Keychain；费用实时预估 |
| 9 | 权限引导 + 热键健康检测 | 1 | 三权限轮询检测；「仅剪贴板模式」降级 |
| 10 | 打包、Developer ID 签名、公证、异常兜底 | 0.5 | ⚠️ 上不了 App Store，直接走独立分发 |

**⭐ 为 v2 留好接口**：M2 就把文本输出层抽象成 `TextSink` 协议，只暴露两个入口：

```swift
protocol TextSink {
    func updatePartial(_ text: String)     // HUD 版 → 悬浮窗；IME 版 → setMarkedText
    func commitDefinite(_ text: String)    // HUD 版 → 注入链；IME 版 → insertText
}
```

这样未来做 IMK 版时，**上游的音频、协议、状态机一行不改**。「专业模式」从架构上就是可插拔的，而不是重写。

### 出口标准（自用可交付）

- [ ] 按住右 ⌘ 说话，松开后文字出现在光标处，**首字一个不丢**
- [ ] HUD 实时显示识别中的文字（灰字预览 + 实心定稿）
- [ ] 在 M0.5 的五个基准 App 中都能正常上屏
- [ ] 连说 3 分钟不崩、不卡、内存不涨
- [ ] **运行 8 小时后热键仍然灵**（验证 tap 自动重启）
- [ ] 断网 / Secure Input / Key 失效都有明确提示，不假死
- [ ] **你自己连续用一周，且不想切回键盘**

> **M2 结束时最重要的事：自己每天用。** 所有真实痛点都会在这一周暴露出来，比任何需求文档都准。

---

## M3 · 打磨到「别人愿意用」（5~7 天）

> **目标**：从「我能用」到「我敢发给别人」。

| 任务 | 说明 |
|---|---|
| **热词/术语库** | 用户词表 → `corpus.context`。⭐ 专有名词识别率的最大杠杆 |
| **替换词典** | 正则/精确替换，纠正固定错误 |
| **LLM 润色** | 接豆包/Claude，口语→书面语。可选自动或快捷键触发 |
| **点按开关模式** | 长文口述不用一直按着 |
| **历史记录** | 最近 20 条，可复制/重新上屏。识别错了不用重说 |
| **用量统计** | 本月说了多少分钟 ≈ 多少钱。BYOK 模式下这是刚需 |
| **App 兼容性清单** | 逐个测 15+ 个常用 App，建表记录，修长尾 |
| **错误文案人性化** | 所有错误码翻译成人话 + 操作建议 |
| **首次启动引导** | 权限 + 试说一句的 onboarding |

### 出口标准

> 发给 3~5 个朋友（最好是 AI 重度用户），他们能**自己装好、自己配好 Key、用起来不来问你**。

---

## M4 · 差异化（2~3 周）

按优先级：

### 4.1 IMK 输入法形态（最大差异化，也最有风险）

**第一步永远是 Spike（1~2 天），不要直接开发**：

```
[ ] 建最小 IMK 输入法工程，装到 /Library/Input Methods
[ ] Timer 每 300ms 主动 setMarkedText 递增文本
[ ] 在 备忘录/VSCode/iTerm2/Chrome 观察是否正常渲染与替换
[ ] 在输入法进程里起 AVAudioEngine，看能否拿到麦克风权限
```

| Spike 结果 | 下一步 |
|---|---|
| 全部通过 | 直接把 `VoiceCore` 接进去，2 周做出输入法版 |
| 麦克风不通 | 改为「输入法 App + 后台守护进程 + XPC」双进程架构，+1 周 |
| 主动 setMarkedText 不通 | **放弃 IMK 形态**，形态 A 已经够好，把精力投到 4.2/4.3 |

### 4.2 场景化 Profile

检测前台 App，自动切换配置：

| 前台 App | 热词集 | 润色 | 上屏方式 |
|---|---|---|---|
| Claude Code / Cursor / 终端 | 技术术语 | 关（保留原意） | 模拟键入 |
| 飞书 / 微信 | 同事名/项目名 | 开（口语→书面） | 剪贴板 |
| 备忘录 / Obsidian | 个人词库 | 轻度 | 剪贴板 |

> 这是**别人很难抄**的功能，因为它需要真实使用场景的积累。

### 4.3 语音指令编辑

说「删掉最后一句」「改成正式一点」「翻译成英文」→ LLM 识别意图并操作已上屏文本。

### 4.4 本地后备后端

FunASR / Whisper.cpp，用于隐私模式与离线场景。

---

## 全局风险登记

| # | 风险 | 概率 | 影响 | 缓解 | 触发时机 |
|---|---|---|---|---|---|
| 1 | ⭐⭐ **文本注入在真实 App 里大面积不可靠，且失败是静默的** | 高 | **极高** | **M0.5 的 3 天 go/no-go 门槛**；三级策略链；AX 一律写后回读校验不信返回码；Secure Input 主动检测；永远保留剪贴板兜底 + 「粘贴上一段」快捷键；**严禁退格回改** | **M0.5** |
| 2 | ⭐⭐ **热键长时间运行后无声失灵**（两个成因：tap 被系统静默禁用 + TCC 绑定代码签名） | 高 | 高 | 回调里捕获 `tapDisabledByTimeout/ByUserInput` 立即 `tapEnable`；周期性校验 `tapIsEnabled`；轮询 `AXIsProcessTrusted()`；签名与安装路径定死；HUD 上给「热键健康」指示 | M2 |
| 3 | ⭐⭐ **被豆包输入法正面碾压** | 高 | **极高** | **立项前先装豆包输入法实测**，量化首字延迟/掉字率/中英混说作基线。**若你的差异点它已做到 3 个以上，应重新评估立项** | **立项前** |
| 4 | ⭐ **协议在文档/实现/路线之间三方不一致** | 中 | 高 | M0 先写抓包 CLI；定型一条路线写死；解码器按 flags 位动态解析不写死偏移；错误帧先 dump 原始字节 | M0 |
| 5 | **首字丢失** | 高（若不处理） | 高（信任崩塌） | 环形音频预缓冲 —— **M2 必须做对**，这同时是差异化支点 | M2 |
| 6 | **2.0 Resource ID 无人验证** | 中 | 中 | M0 实测；不通则退回 1.0（代价：4.5 元/小时） | M0 |
| 7 | **typeflux 已占住「非 IME + 隐私自持」** | 已发生 | 中 | 叠加可编程热词 + 首字不丢 + AI 编程场景 | 持续 |
| 8 | macOS 三权限劝退用户 | 高 | 中 | 引导页轮询自动前进；「仅剪贴板模式」降级 | M3 |
| 9 | 计费口径判断错误 | 中 | 中 | 对照实验反推；会话级 VAD 门控；内置用量与费用显示 | M0/M3 |
| 10 | IMK 麦克风权限拿不到 | 中 | 中（仅影响 P2） | 直接按双进程架构设计（主 App 采音 + IME 助手上屏 + XPC） | M4 |

---

## ⚠️ 需要你拍板的六个问题

这些是调研中发现的**真实矛盾**，不拍板会导致返工：

| # | 问题 | 建议 |
|---|---|---|
| 1 | **形态之争**：非 IME（不占输入法槽，最干净的差异化）vs IMK（唯一能做真逐字上屏）—— **两者互斥** | 先非 IME，`TextSink` 抽象留好 IMK 接口 |
| 2 | **上屏最小单位是句还是短语？** 直接决定 `end_window_size`（300~500 vs 1500~3000），两个方向相反 | 做成「快嘴/思考」两档用户可选 |
| 3 | **VAD 门控粒度**：包级省钱但会污染流式 cache 导致乱码；会话级安全但省得少 | **会话级** |
| 4 | **目标用户**：愿意自己配 Key 的开发者 vs 普通 Mac 用户 —— BYO Key 模式只对前者成立 | 收窄到开发者/重度口述者 |
| 5 | **最低系统版本**：豆包/讯飞支持 10.15+ 双架构；卡 14+ 或 Apple-Silicon-only 会让出老 Intel 用户 | 视目标用户而定 |
| 6 | **开源还是闭源** —— 决定能抄谁（VoiceInk 是 GPL-3.0，type4me/Handy/openless 是 MIT） | — |

---

## 时间线汇总

| 里程碑 | 工期 | 累计 | 产出 |
|---|---|---|---|
| M0 协议验证 | 1~2 天 | 2 天 | 抓包型客户端 + 四段延迟实测表 |
| **M0.5 注入门槛** ⛔ | 3 天 | 5 天 | **go/no-go 决策** |
| M1 CLI 打通 | 3~4 天 | 9 天 | 端到端能用的脚本 |
| **M2 macOS MVP** | 8~10 天 | **19 天** | ⭐ **自用级 App** |
| M3 打磨 | 5~7 天 | 26 天 | 可分发的 App |
| M4 差异化 | 2~3 周 | 7~8 周 | 有壁垒的产品 |

> **约 4 周做出自己天天用的东西，约 6 周做出能发布的东西。**
> （比初版估算多 3 天，因为加了 M0.5 这个不可跳过的门槛）

---

## 立刻可以做的三件事

### ① 先装豆包输入法，实测你的对手（半天，最重要）

macOS 版 2026-05-12 已上线，免费。装上实测并记录：

- 首字延迟有多少？**是否真的会漏掉开头几个字？**（这是你的差异化支点之一，要确认它真的存在）
- 逐字上屏的节奏与跳变程度
- 中英混说：说「用 Claude Code 写个 WebSocket 客户端」，看 Claude 会不会变成 Cloud
- 它要不要登录、占不占输入法槽、要不要辅助功能权限

> **如果它已经做到了你规划中的三个以上差异点，先别写代码，回来重新想定位。**

### ② 克隆两个最相关的开源项目读一遍（半天）

```bash
git clone https://github.com/joewongjc/type4me      # 1.4k star, MIT, 直接对标物
git clone https://github.com/mylxsw/typeflux        # Swift 协议实现最完整
```

重点读 `Type4Me/ASR/VolcASRClient.swift` 和 `Sources/Typeflux/STT/DoubaoRealtimeTranscriber.swift`。
**这两份代码能让你的 M0 从 2 天缩到半天。**

### ③ 开通服务、跑通协议

```bash
open https://console.volcengine.com/speech/service
```

开通「豆包流式语音识别模型 2.0」，然后把 [02 章 §9](02-豆包流式ASR接入.md) 的代码存成 `spike-python/sauc_client.py`，
喂一个 16k 单声道 wav —— **今天就能看到第一行字。**
