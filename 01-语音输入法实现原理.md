# 语音输入法的实现过程与实现逻辑

> 本章讲清楚「一个语音输入法内部到底发生了什么」，与具体厂商无关。
> 豆包/火山的具体接入见 [02-豆包流式ASR接入.md](02-豆包流式ASR接入.md)。

---

## 0. 一句话概括

语音输入法 = 一条实时管线

```
触发 → 采集 → 前处理/VAD → 分帧编码 → 流式上传 → 增量解码
     → 文本稳定化 → 后处理 → 上屏注入
```

外加一套控制面（状态机、权限、异常、计费/省流）。

其中**只有两处是真正的难点**，其余都是工程活：

| 难点 | 本质 | 在哪一层 |
|---|---|---|
| **文本回退**：识别结果会不断改写已经吐出的字 | 流式解码的 hypothesis 不稳定 | 文本稳定化层 |
| **上屏注入**：怎么把字写进「任意 App 的任意输入框」 | 操作系统的输入体系限制 | 上屏注入层 |

新手常以为难点在「识别准不准」——那是模型厂商的事，现在已经是买来即用的商品。

---

## 1. 十层管线拆解

### Layer 0 · 触发层（Trigger）

决定「什么时候开始听、什么时候停」。四种范式：

| 范式 | 交互 | 优点 | 缺点 | 代表 |
|---|---|---|---|---|
| **Push-to-Talk 按住说话** | 按住 Fn / 右⌘ / CapsLock，松开结束 | 零误触发、边界明确、省钱 | 手要一直按着，长文累 | CapsWriter、FnKey、VoiceInk |
| **Toggle 点按开关** | 敲一下开始，再敲一下结束 | 解放双手 | 忘关会空跑烧钱 | superwhisper |
| **免提常开 + VAD 自动断句** | 一直听，靠静音判停 | 最像「说话就出字」 | 误触发、耗电、计费高 | 会议转写类 |
| **唤醒词** | "小X小X" | 完全免手 | 中文唤醒误报率高，输入法场景没必要 | 智能音箱 |

> **工程建议**：MVP 只做 Push-to-Talk。它把「音频边界」这个最脏的问题直接消灭了——不用猜用户什么时候开始说、什么时候说完。Toggle 作为第二模式，免提模式留到 V2 再说。

**热键实现的坑**（macOS）：
- Fn 键不是普通 keyCode，它走 `flagsChanged` 事件，必须用 `CGEventTap` 监听，且需要**输入监控（Input Monitoring）**权限。
- `NSEvent.addGlobalMonitorForEvents` 拿不到键盘事件的「消费权」（无法拦截），只能观测；要吞掉按键必须用 CGEventTap。
- CapsLock 更麻烦：系统会真的切换大小写状态，需要额外抑制。

---

### Layer 1 · 音频采集（Capture）

目标产物：**16 kHz / 16 bit / 单声道 / 小端 PCM**（这是几乎所有云端 ASR 的标准输入）。

现实是麦克风设备大多跑在 44.1 kHz 或 48 kHz 立体声，所以必须自己**重采样 + 降混**。

```
设备原始流 (48kHz f32 stereo)
  → 降混 mono
  → 重采样 48k→16k
  → f32 → s16le 量化
  → 环形缓冲 (RingBuffer)
  → 按 200ms 切片取走
```

平台 API 选型：

| 平台 | 推荐 | 备注 |
|---|---|---|
| macOS | `AVAudioEngine` + `AVAudioConverter` | 最省事，converter 直接做重采样；`installTap` 拿 buffer |
| macOS（更底层） | AudioQueue / CoreAudio HAL | 需要精确控制缓冲区大小时才用 |
| 跨平台 Rust | `cpal` + `rubato` | Handy / FnKey 走这条 |
| 跨平台 Python | `sounddevice` / `pyaudio` | 原型最快 |
| Electron | `getUserMedia` + `AudioWorklet` | VoiceVibe 走这条，注意 worklet 里做重采样 |

**必须处理的边界情况**：

1. **蓝牙耳机（AirPods）**：一旦启用麦克风，会从 A2DP 掉到 HFP/SCO，采样率骤降到 16 kHz 甚至 8 kHz，音质明显变差且**整机音频输出也会跟着降质**。用户会以为是你的 App 把音乐搞坏了。要么提示、要么优先用内置麦克风。
2. **设备热插拔**：录音中途插拔耳机 → 采样率变化 → 必须重建 engine，否则拿到的是垃圾数据。
3. **麦克风被独占**：某些会议软件会独占设备。要有降级路径与明确报错。
4. **首帧延迟**：`AVAudioEngine` 冷启动有 100~300 ms 开销。**热键按下才启动引擎会丢掉开头的字**。
   → 解法：**预热**。热键按下前就让引擎空转（或在 App 启动时常驻），按下瞬间只是「开始把 buffer 往外送」。这是「第一个字总是丢」这个经典 bug 的根治方案。

---

### Layer 2 · 前处理与 VAD

**VAD（Voice Activity Detection，语音活动检测）在语音输入法里有三个完全不同的用途，别混为一谈：**

| 用途 | 说明 | 对精度的要求 |
|---|---|---|
| **省钱/省流** | 静音段不推流，因为云端 ASR 按**音频时长**计费 | 中，宁可多发不可漏发 |
| **自动断句** | 判停 → 触发一次「确定分句」 | 高，直接影响上屏节奏 |
| **防误触发** | 免提模式下过滤环境噪声 | 高 |

> ⚠️ **省钱要用「会话级门控」而不是「数据包级门控」** —— 这是一个反直觉但已被实战验证的坑：
>
> 「保持连接、只把静音的包丢掉不发」会**污染流式模型的 cache，导致乱码与重复字**（`casperkwok/VoiceAgent` 的实测反例，修复方式是改回连续喂）。
>
> 正确做法：**说话期间连续喂满**；静音超过阈值（如 1.5 s）就**直接发末包收尾并断连**。
> 详见 [02 章 §10.8](02-豆包流式ASR接入.md)。

> ⚠️ **关键认知**：如果用的是豆包这类云端大模型 ASR，**断句这件事应该交给服务端做**（它有 `end_window_size` / `vad_segment_duration` 参数和语义分句能力，比本地 VAD 聪明得多）。本地 VAD 只负责「省钱」和「防误触发」。自己在本地做断句再切分连接，反而会切断语义上下文，识别质量下降。

端侧 VAD 选型：

| 方案 | 体积 | 延迟 | 准确率 | 评价 |
|---|---|---|---|---|
| **WebRTC VAD** | ~50 KB | <1 ms | 中（噪声环境差） | 老牌，纯能量+GMM，够用且极轻 |
| **Silero VAD (ONNX)** | ~1.8 MB | ~1 ms/30ms帧 | 高 | **首选**，鲁棒性远好于 WebRTC，几乎零成本 |
| **TEN VAD** | ~300 KB | 极低 | 高 | 较新，专为实时对话优化，值得关注 |
| **FSMN-VAD (FunASR)** | ~1.5 MB | 低 | 高 | 中文场景调优好，但依赖较重 |

**降噪要不要做？**

> **默认不要做。** 云端大模型 ASR（豆包 2.0 / Seed-ASR）是在真实带噪数据上训练的，本身抗噪能力很强。前置 RNNoise/DeepFilterNet 这类降噪会**引入频谱失真，反而降低识别率**——这是很多人踩过的坑。
> 例外：极端噪声环境（车载、工厂）或远场拾音，可以考虑，但必须 A/B 实测 CER 再决定。
>
> AGC（自动增益）同理，系统层面通常已经做了，别叠加。

---

### Layer 3 · 分帧与编码

**分包大小是有讲究的，不是越小越好。**

- 太小（<50 ms）：包头开销占比高、请求数暴涨、服务端每包都要跑一次解码，反而拖慢。
- 太大（>500 ms）：首字延迟直接被拉长，「边说边打字」的实时感消失。

火山官方明确建议：**100~200 ms/包，双向流式模式下 200 ms 性能最优**。

编码选择：

| 方案 | 码率(16k单声道) | 评价 |
|---|---|---|
| **raw PCM s16le** | 256 kbps | **首选**。国内网络下带宽完全不是瓶颈，零编码延迟、零复杂度 |
| PCM + gzip | ~200 kbps | 火山协议支持，但 PCM 压缩率很差，收益小、CPU 有开销 |
| Opus (ogg 容器) | 16~32 kbps | 省 90% 带宽，但增加 ~20 ms 编码延迟和依赖。移动端弱网才值得 |

> **结论**：桌面端 + 国内网络，直接 raw PCM。200 kbps 对语音输入这种「每次说几十秒」的场景，一小时也才 100 MB 量级，且实际只在说话时才发。

---

### Layer 4 · 传输层

WebSocket 是行业标准（HTTP/2 gRPC 双向流也有，但云厂商基本都提供 WS）。

需要在这一层解决：

1. **连接生命周期**：一次「按住说话」= 一条连接？还是长连接复用？
   - 一次一连：简单干净，但每次都要付 TCP+TLS+WS 握手 ~100-300 ms → **首字延迟被拉长**。
   - **连接预热/池化**：热键按下前就把连接建好挂着（或维持一个空闲连接池），按下瞬间直接发首包。**强烈推荐**，能砍掉几百毫秒。注意服务端会有空闲超时（「等包超时」错误码），需要心跳或者按需重建。
2. **背压**：网络抖动时音频还在源源不断产生，必须有缓冲队列 + 丢弃策略（丢最老的，而不是阻塞采集线程）。
3. **重连**：断线后是否续传？流式 ASR 通常无法续传上下文，只能重开并把已确定的文本保留、未确定的丢弃。
4. **鉴权**：token 绝不能硬编码在客户端（见 04 章的安全设计）。

---

### Layer 5 · ASR 流式解码

这一层是买来的。你需要理解的是它**吐什么**：

```jsonc
// 典型的流式返回（豆包 sauc 格式）
{
  "result": {
    "text": "我要去北京出差",          // 到目前为止的全量文本
    "utterances": [
      {
        "text": "我要去北京出差",
        "start_time": 0, "end_time": 2100,
        "definite": false,              // ← 关键：这句还没定稿
        "words": [ {"text":"我","start_time":740,"end_time":860}, ... ]
      }
    ]
  },
  "audio_info": { "duration": 2100 }
}
```

三个必须理解的概念：

| 概念 | 含义 | 用途 |
|---|---|---|
| **partial / 中间结果** | `definite: false`，随时会被改写 | 用于**预览**，不能直接当最终文本 |
| **definite / 确定分句** | `definite: true`，服务端保证不再改 | 用于**提交上屏** |
| **utterance / 分句** | 一个语音片段的完整单元，带时间戳 | 断句、逐句上屏的基本单位 |

---

### Layer 6 · 文本稳定化 ⭐ 核心难点之一

**问题**：流式 ASR 每 200 ms 给你一个「当前最佳猜测」，这个猜测会不断推翻自己。

真实的时间线长这样：

```
t=0.40s   "我"
t=0.60s   "我要"
t=0.80s   "我要去"
t=1.00s   "我要去被"          ← 猜错了
t=1.20s   "我要去北京"         ← 自我修正，"被"变"北京"
t=1.60s   "我要去北京出差"
t=2.40s   "我要去北京出差。"    ← definite，加了标点
t=2.40s   "我要去北京出差。"    ← 若开 ITN，"二十号" 会变 "20号"，长度突变
```

如果你天真地「收到什么就往输入框里打什么」，用户会看到文字疯狂闪烁、回退、重排——**体验灾难，而且在很多 App 里会把光标搞乱**。

**三种上屏策略**：

| 策略 | 做法 | 首字延迟 | 稳定性 | 实现难度 | 适用 |
|---|---|---|---|---|---|
| **A · 只上屏 definite** | 中间结果只在**悬浮窗**预览，`definite:true` 才写进输入框 | 高（等判停 500~800 ms） | ✅ 完美，永不回退 | ⭐ 低 | **MVP 首选** |
| **B · 预编辑上屏**（输入法路线） | 中间结果用 IME 的 marked text（带下划线的预编辑区）实时显示在光标处，definite 时 commit | 低（~500 ms） | ✅ 好，预编辑区天然可整体替换 | ⭐⭐⭐ 高（要写输入法） | **终态形态** |
| **C · 退格重写** | 直接往输入框打字，变化时发 N 个 Backspace 再补新字 | 低 | ❌❌ **破坏性** | ⭐⭐ 中 | **禁用** |

> ⛔ **策略 C 的最终裁定：MVP 严禁使用，只允许追加。**
>
> 风险不可接受：你不知道这期间用户有没有自己动过光标、点了别处、App 做了自动补全或自动大写、输入法把字符重组了。**一旦 N 算错就会删掉用户自己的文字，这是不可逆的破坏性操作。** 且 Electron/终端里模拟按键可能丢帧，删除数量与实际不符。
>
> **已实测佐证**：`typeflux` 把豆包协议写到 1149 行含单测，其 `AXTextInjector.swift` 仍**只提供整段 `insert()`/`replaceSelection()`，不做任何 partial 替换跟踪** —— 说明非 IME 路线上确实没有安全解。

**为什么 B 是「真·边说边打字」的唯一正解**：
操作系统的输入法体系里，预编辑区（marked text / composition string / preedit）本来就是为「未定稿文本」设计的——拼音输入法打字时那段带下划线的内容就是它。它可以被**整体替换**，不涉及退格，不会破坏 App 的撤销栈，且所有正经输入框都原生支持。语音识别的中间结果和拼音的候选串，在语义上是同一个东西。

**增量 diff 算法**（策略 C 或者需要更新已提交文本时用）：

```
function computeEdit(prev, next):
    # 求最长公共前缀
    i = 0
    while i < len(prev) and i < len(next) and prev[i] == next[i]:
        i += 1
    backspaces = len(prev) - i     # 需要退掉的字符数
    insertion  = next[i:]          # 需要新打的字符
    return (backspaces, insertion)
```

> 注意：中文 + emoji 要按 **Unicode 字素簇（grapheme cluster）** 而非 UTF-16 code unit 计数，否则退格数会算错。

**如果 ASR 不提供 definite 标记**（比如自建 Whisper 流式），就需要自己做稳定前缀判定，业界标准做法是 **LocalAgreement-n**：

> 连续 n 次（通常 n=2）解码结果的公共前缀，视为「稳定」，可以提交。
> 出自 `ufal/whisper_streaming`，是自建流式 ASR 的必备算法。

**好消息**：豆包 sauc 原生提供 `definite` 标记和 `enable_nonstream` 二遍识别，这块可以直接省掉。

---

### Layer 7 · 后处理

| 能力 | 说明 | 豆包是否内置 |
|---|---|---|
| **标点** | 「今天天气不错」→「今天天气不错。」 | ✅ `enable_punc` |
| **ITN 文本规范化** | 「二零二六年」→「2026年」、「一百二十三块」→「123元」 | ✅ `enable_itn` |
| **语义顺滑 DDC** | 去掉「嗯」「那个」「就是说」等口水词与重复 | ✅ `enable_ddc` |
| **热词/偏置** | 让专有名词（人名、产品名、术语）识别更准 | ✅ `corpus.context` 直传 / 热词表 |
| **替换词** | 强制把 A 换成 B（纠正固定错误） | ✅ 替换词表 |
| **繁简转换** | 简→繁/台湾正体/香港繁体 | ✅ `output_zh_variant` |
| **敏感词过滤** | 过滤/替换为 * | ✅ `sensitive_words_filter` |
| **LLM 润色** | 口语→书面语、改写、翻译、按指令编辑 | ❌ 需自己接大模型 |

> ⚠️ **ITN 与流式的冲突**：ITN 会让文本长度突变（「二零二六」4字 → 「2026」4字符但字素不同）。如果你在做退格重写，这会导致 diff 计算出巨大的编辑距离，屏幕上闪一大片。
> → 解法：**中间结果关闭 ITN/标点，只在 definite 结果上开**。或者干脆走预编辑路线（策略 B），整体替换不受影响。

---

### Layer 8 · 上屏注入 ⭐ 核心难点之二

「怎么把字送进当前光标所在的输入框」——这是**平台强相关**的部分，也是所有开源项目做得最参差的部分。

macOS 四条路线（详细兼容性对比与代码见 [04-技术实现方案.md](04-技术实现方案.md)）：

| 路线 | API | 实时改写能力 | 兼容性 | 权限 |
|---|---|---|---|---|
| **a. 输入法（IMK）** | `IMKInputController` + `setMarkedText` / `insertText` | ✅ 原生支持预编辑与替换 | 极好（所有正经输入框） | 无需 TCC，但用户要主动切到你的输入法 |
| **b. 模拟键入** | `CGEvent.keyboardSetUnicodeString` | ⚠️ 只能靠退格 | 好，但长文本易丢字 | 辅助功能 |
| **c. 辅助功能直写** | `AXUIElement` + `kAXSelectedTextAttribute` | ✅ 可精确替换 range | ❌ 差，Electron/终端/浏览器大量不支持 | 辅助功能 |
| **d. 剪贴板 + ⌘V** | `NSPasteboard` + 模拟按键 | ❌ 只能整段贴 | 极好，几乎无死角 | 辅助功能 |

**实践共识**（几乎所有开源项目的最终选择）：
> **短文本用 (b) 模拟键入，长文本用 (d) 剪贴板粘贴（贴完恢复原剪贴板内容）**，(c) 作为可选优化，(a) 作为终极形态单独做一个 App。

---

### Layer 9 · 控制面

**状态机**（这是让产品「不出诡异 bug」的关键）：

```
        ┌──────────┐
        │   IDLE   │◄──────────────────────┐
        └────┬─────┘                       │
             │ 热键按下                     │
        ┌────▼─────────┐   权限缺失/设备失败  │
        │  PREPARING   ├──────────────────►│
        │ (预热/建连)   │                   │
        └────┬─────────┘                   │
             │ 就绪                        │
        ┌────▼─────────┐                   │
        │  LISTENING   │  ← 采集+推流+收partial
        └────┬─────────┘                   │
             │ 热键松开 / VAD判停 / 超时     │
        ┌────▼─────────┐                   │
        │  FINALIZING  │  ← 发最后一包，等最终结果
        └────┬─────────┘                   │
             │ 收到 definite / 超时兜底      │
        ┌────▼─────────┐                   │
        │  INJECTING   │  ← 上屏             │
        └────┬─────────┘                   │
             └───────────────────────────────┘
```

**必须有的兜底**：
- `FINALIZING` 必须有**超时**（如 3 s）。服务端不返回最终包时，用已有的最佳文本上屏，绝不能卡死。
- 用户在 `LISTENING` 时切换了前台 App → 上屏目标变了，要么锁定原目标，要么取消。
- 全局「紧急取消」（如 Esc）：丢弃本次，什么都不上屏。

---

## 2. 延迟预算（这是产品体感的全部）

以豆包双向流式为例，从「用户说出第一个字」到「屏幕上出现第一个字」：

| 环节 | 典型耗时 | 可优化手段 |
|---|---|---|
| 音频引擎启动 | 100~300 ms | **预热常驻** → ~0 |
| WebSocket 建连 + 握手 | 100~300 ms | **连接预热/池化** → ~0 |
| 分包缓冲 | 200 ms | 首包可以缩到 100 ms |
| 网络 RTT（国内） | 20~60 ms | 就近接入 |
| ASR 首字解码 | 300~500 ms | `enable_accelerate_text` 可再压，代价是准确率 |
| 上屏注入 | <10 ms | — |
| **首字总延迟** | **~600~800 ms**（已优化） / ~1.2 s（未优化） | |

尾字/整句延迟（definite）：
`end_window_size`（判停静音阈值，默认 800 ms，可调到 500）+ 二遍识别 ~200~300 ms ≈ **0.7~1.1 s**

> **产品结论**：600~800 ms 的首字延迟，配合悬浮窗实时预览，用户体感就是「说着说着字就出来了」。低于 500 ms 用户会觉得「快得离谱」，高于 1.5 s 就会明显觉得「卡」。

---

## 3. 云端 vs 端侧的路线取舍

| 维度 | 云端（豆包/火山） | 端侧（Whisper / FunASR / Parakeet） |
|---|---|---|
| 中文准确率 | ⭐⭐⭐⭐⭐ 顶级，方言、口音、专有名词强 | ⭐⭐⭐ Whisper 中文一般，FunASR 尚可 |
| 首字延迟 | 600~800 ms | 端侧流式难做，多数开源项目是**说完才转**（1~3 s） |
| 真流式 | ✅ 原生 | ⚠️ 需要自己做 chunk + LocalAgreement，工程量大 |
| 成本 | 1 元/小时（说话时长） | 0，但吃 CPU/GPU、耗电 |
| 隐私 | ❌ 音频出本机 | ✅ 完全本地 |
| 离线 | ❌ | ✅ |
| 上下文/热词 | ✅ 可传对话历史、图片上下文 | 有限 |

> **结论**：要做「边说话边打字」这个体验，**云端流式是唯一现实选项**。
> 开源生态里 90% 的语音输入工具是「录完 → 整段转写」，正是因为端侧流式太难做好。这也恰恰是你这个项目的差异化空间（见 [05-产品设计方案.md](05-产品设计方案.md)）。
> 隐私敏感场景可以保留一个本地 Whisper 后端作为可选项，但别当主路径。

---

## 4. 一张图看完整链路

```
 ┌─ 热键(Fn) ─────────────────────────────────────────────┐
 │                                                        │
 ▼                                                        │
[采集 AVAudioEngine] ──48k f32──► [降混+重采样] ──16k s16──►[RingBuffer]
                                                            │
                                        每 200ms 取一片      │
                                                            ▼
                                                     [Silero VAD]
                                                      静音? ─是─► 丢弃(省钱)
                                                            │否
                                                            ▼
                                              [二进制协议封包 + WS 发送]
                                                            │
                                                     wss://openspeech…
                                                            │
                                                            ▼
                                              [豆包 sauc 流式解码]
                                                            │
                                        partial ◄───────────┤───────► definite
                                           │                            │
                                           ▼                            ▼
                                   [悬浮窗实时预览]              [文本稳定化/diff]
                                                                        │
                                                                        ▼
                                                            [后处理: 热词替换/LLM润色]
                                                                        │
                                                                        ▼
                                                    [上屏: IME预编辑 / 模拟键入 / 剪贴板]
```

---

## 5. 小结：做这件事你真正要写的代码

| 模块 | 工作量 | 风险 |
|---|---|---|
| 音频采集 + 重采样 + 环形缓冲 | 2~3 天 | 低（有成熟 API） |
| 火山 sauc 二进制协议客户端 | 2~3 天 | 中（协议细节多，见 02 章） |
| 状态机 + 热键 + 权限引导 | 3~4 天 | 中（macOS TCC 权限体验很坑） |
| 上屏注入（模拟键入 + 剪贴板） | 3~5 天 | **高**（App 兼容性长尾） |
| 悬浮窗 HUD | 2~3 天 | 低 |
| 输入法（IMK）版本 | 2~3 周 | **高**（生态里没有现成先例） |
| 设置界面 / 热词 / LLM 润色 | 1 周 | 低 |

**MVP（不含输入法）约 2~3 周可跑通**，详见 [06-实现路径与里程碑.md](06-实现路径与里程碑.md)。
