# Codex 项目级强制规则

以下规则适用于本项目中的所有分析、实现、修复和验收工作，优先级高于一般开发习惯。

## 1. 禁止猜测，必须明确确认

- 不得根据表象、经验或单张截图直接推断功能和布局已经正确。
- 修改前必须通过源码、运行状态、日志、接口响应、控件树或截图等可验证证据确认问题与根因。
- 修改后必须进行与风险相匹配的编译、运行和实际交互验证。
- 对尚未实际验证的状态必须明确标记为“未验证”，不得声称已经修复或完成。
- 如果权限、环境、外部服务或系统弹窗阻止验证，必须说明阻塞点并停止下结论。

## 2. 禁止随意改代码，修改前全面 Review

- 改动任何模块前，必须先完整 Review 该模块的入口、容器、共享状态、调用关系、相关组件和现有改动。
- 必须搜索同类实现和所有调用点，确认改动不会与系统行为、公共组件或其他页面重复、冲突。
- 修改范围保持最小，只处理已确认的问题；不得顺手重构、调整无关布局或覆盖用户已有改动。
- 修改后必须检查 diff，并回归受影响的相邻功能和状态，避免“修复一个问题，又制造另一个问题”。
- UI 修改不得只通过编译作为完成标准，必须尽可能启动实际软件进行视觉和交互 Review。

## 3. 页面布局必须进行全局分析

- 不得只针对单个页面、单个 Tab 或单一截图增加局部 padding、offset、safe-area 等补丁。
- 修改布局前必须分析完整窗口结构，包括标题栏、工具栏、交通灯、侧边栏、内容区、安全区、浮层和底部区域。
- 必须检查共享布局容器和设计指标，优先在共同根因处修复，避免各页面出现独立布局分支。
- 至少验证所有相关 Tab，以及侧边栏展开/收起、窗口尺寸变化、登录/未登录和主要页面状态。
- 验收时必须同时确认目标问题、全局布局和此前已修复区域均未发生回归。
