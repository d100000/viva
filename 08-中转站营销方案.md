# Viva × 中转站 连带营销方案

> 目标：把免费开源的 Viva 做成 bobdong.cn 中转站的获客渠道，同时不损伤 Viva 自身的产品口碑。
> 状态：v1，2026-07-26。标注 ⬜ 的是待你确认的信息，标注 ✅ 的是已经写进代码的。

---

## 0. 一句话策略

**Viva 卖的不是软件，是「说话变成文字」这件事的完成度。**
而完成度的最后一公里 —— AI 润色 —— 恰好卡在一个所有人都讨厌的环节上：
去云厂商注册、实名、充值、开通模型、建 Key、抄一个大小写敏感的模型名。

中转站不是「顺便推的广告」，它是**这个卡点的解药**。
这决定了整套营销的调性：**不是推销，是替用户把路铺平**。

---

## 1. 为什么这个连带成立

### 1.1 漏斗天然存在

```
下载 Viva（免费开源，无需任何账号）
   ↓  用户为了能用，必须去搞一个语音识别 Key    ← 第一道坎（火山引擎，我们改不了）
开始用「边说边写」
   ↓  用了几天，发现原始识别结果有口水词、断句糙
点开「AI 润色」开关                              ← ⭐ 意图最强的一瞬间
   ↓  发现还要再搞一个大模型 Key                 ← 第二道坎（这道坎我们能拆）
【中转站在这里出现】
```

第二道坎的关键性质：**用户此刻的意图是自己产生的，不是被广告唤起的。**
他刚刚亲手点了「我想要 AI 润色」。这时候给他一条最短路径，是帮忙；
在首页挂个横幅推同样的东西，是打扰。位置决定性质。

### 1.2 用户画像高度重合

会装一个开源语音输入法、愿意自己配 API Key 的人 ——
本身就是「会用大模型 API、但嫌注册麻烦」的那批人。
这是中转站的核心客群，重合度极高，几乎不需要教育成本。

### 1.3 单点足够痛

挨家注册一遍的真实成本（实测）：

| 服务商 | 拦路虎 |
|---|---|
| 火山方舟 | 实名认证 → 还得单独去「开通管理」把模型开通，不开通调用直接失败 |
| 阿里百炼 | 实名 + 开通百炼，Key 与地域绑定，路径是 `/compatible-mode/v1` 填错就 404 |
| DeepSeek | 预付费，余额 0 直接 402；`deepseek-chat` 已下线，模型名得重查 |
| OpenAI / Anthropic | 海外支付方式 |
| 智谱 | 路径 `/api/paas/v4`，temperature 开区间不能传 0 |

**一个 Key 全通** 对应的是省掉上面一整张表。这就是价值主张的全部内容，
不需要再包装。

---

## 2. 产品内转化位（Product-Led Growth）

按用户意图强度排序，**强意图的位置给强引导，弱意图的位置只留一行字**。

### P0 ✅ 已实现：润色开关的即时引导

**位置**：主界面「AI 润色」胶囊按钮 → 用户点开但还没配模型时弹出 popover
**代码**：`app/Sources/Viva/SpeakView.swift` → `PolishSetupPopover`

文案（已上线）：

> **还差一个润色模型**
> 润色要调一个大模型。挨家云厂商注册的话，实名、充值、开通模型、抄模型名，一套下来十几分钟；
> 用 **Viva 中转站**的话，国内外的模型共用一个 Key，填进去点「拉取模型」就自动配好了。
> 　[ 去拿一个 Key ]　[ 已经有 Key 了 ]

为什么是 popover 不是弹窗：弹窗要用户"处理"，popover 点外面就消失。
这个位置一天可能触发一次，必须做到不烦人。

### P0 ✅ 已实现：设置页的转化卡片

**位置**：设置 → 大模型润色 → 服务商选择器**正上方**
**代码**：`app/Sources/Viva/SettingsView.swift` → `RelayPromoCard`
**显示条件**：当前没在用中转站时才显示（用了就消失，不做二次骚扰）

> ⚡ **懒得挨家注册？用 Viva 中转站**　`一个 Key 通国内外`
> 国内外主流模型共用一个 Key、一个地址，按量计费、价格很低，也不需要海外支付方式。
> 填好 Key 点「拉取模型」，可用模型自动列出来 —— 不用抄模型名，不用查文档。
> 　[ 用中转站配置 ]　[ 看看价格和支持的模型 ]

「用中转站配置」一次性把**服务地址、协议格式、端点路径**全填好，只留 API Key 一个空 ——
**每少一个要填的框，转化率就抬一截**，而这三个字段填错任何一个都是直接 404。

### P0 ✅ 已实现：自动拉取模型列表（真正的护城河）

**代码**：`app/Sources/Viva/ModelCatalog.swift`

这一条看起来是功能，其实是最强的营销资产，理由有三：

1. **它让"一个 Key 通国内外"从口号变成可验证的事实。**
   用户填完 Key 点一下，屏幕上唰地列出几十上百个模型 ——
   这比任何文案都有说服力，而且是用户自己触发的。
2. **它对所有服务商都开放**（不是只给中转站开的特权），
   所以它是诚实的功能而不是暗桩。也正因为诚实，用户对比时会发现：
   别家拉出来三五个，中转站拉出来一大片。**让事实做对比，不要自己吹。**
3. **它顺手解决了模型名 churn 的老问题** —— 预设表永远会过期，在线拉的永远不会。

已做的细节：过滤掉 embedding / rerank / tts / 画图 这类选了必定调用失败的条目；
把 `mini/flash/lite/nano/turbo/air/haiku` 这类轻量档排到最前并标「润色首选」——
**润色一两句话本来就不需要大模型，帮用户省钱会换来信任**。

### P1 ⬜ 建议增加：润色失败时的兜底提示

用户当前服务商报 402（余额不足）/ 401（Key 过期）/ 404（模型下线）时，
错误提示里加一行：「换个不用操心余额和模型下线的方案？」+ 链接。

**这是转化率最高的时刻** —— 用户刚被别家坑了。
但必须克制：只在**明确是服务商侧的错误**时出现，网络错误、超时一律不提。
否则会变成"你们家出问题就推销"，反噬极大。

### P1 ⬜ 建议增加：欢迎引导的第 4 步（可跳过）

现有引导是 3 步（填语音 Key → 授权 → 试一次）。
加一个**明确标注「可选」**的第 4 步：「想让 AI 顺一遍文字？两种方式…」
并列给出「用中转站（1 步）」和「自己去云厂商建（5 步）」，把步骤数直接摆出来。

⚠️ 必须可跳过、必须默认不选中。**新用户还没体验到价值就被推销，是最差的时机。**

### P2 ⬜ 建议增加：统计页的「本月润色消耗」

如果中转站有余额查询接口，在统计页显示「本月润色 N 次 / 约 X 元」。
让成本可见 → 用户发现真的很便宜 → 自发口碑。⬜ 需要确认接口是否存在。

---

## 3. 分寸：三条不能越的线

开源项目里塞商业推广，**翻车的从来不是"推了"，而是"推得不诚实"**。

### 线 1：默认可以指向自己，但必须是「明的」

**决策（2026-07-26，用户拍板）：`polishProvider` 默认值 = Viva 中转站。** ✅ 已实现

我最初的建议是不改默认值，理由是开源项目的默认值一旦被发现指向作者的收费服务，
issue 区容易炸。用户选择了改。这个选择是合理的 —— 默认值本来就该指向**对新用户最省事的那个**，
而中转站客观上是唯一「填一个 Key 就能用」的选项。真正的问题从来不是"有没有偏好"，
而是"这个偏好是不是藏着的"。

所以配套的**披露义务从"应该做"升级为"必须做"**，三处都已落地：

- ✅ 服务商说明里直写：「这是本工具的默认选项，由本项目作者运营，也是本项目的收入来源
  —— 润色文本会经过它中转（音频不会）」，并给出 Ollama / 换服务商两条替代路径
- ✅ 设置页「数据与隐私」分层表述（见下）
- ✅ README 同样自曝

仍然守住的边界：

- ❌ 不隐藏、不用模糊措辞（「由合作伙伴提供」这类说法比不写更糟）
- ❌ 不因为用户换了服务商就降级功能、加提示、反复劝回
- ✅ 只影响**全新安装**；老用户配置里已存了 `polishProvider`，不会被静默改写
  （`Config.init(from:)` 是手写的容错解析，不会覆盖已有值）
- ✅ 润色本身默认**关闭**。默认值只决定"如果你要用润色，默认走哪家"，
  而不是默认就把你的文本发出去 —— 这条比什么都重要

### 线 2：隐私话术必须重写 ⚠️ 最高优先级

**这是整个方案里唯一可能真正伤到 Viva 的地方。**

Viva 设置页现在写着：

> 「音频只发往你自己配置的火山引擎账号，**不经过任何第三方服务器**。」

一旦用户把润色配到中转站，**润色的文本就经过中转站了**。
这句话在那一刻就不再成立 —— 而它恰恰是我们自己写的信任承诺。

被人发现（一定会被发现，代码是开源的）的后果，比多赚的那点钱严重得多。

**必须改成分层表述**：

> **语音**：音频只发往你自己配置的火山引擎账号，不经过任何第三方。
> **润色**：文本会发往你在上面配置的那家大模型服务。
> 　　　　如果你选了 Viva 中转站，文本会经过 bobdong.cn 中转到目标模型 —— 中转站由本项目作者运营。
> 　　　　介意的话，选任意其它服务商，或用 Ollama 完全本地润色。

主动说、说在前面、并且**给出替代方案**。
这反而是最强的信任建设：一个肯自曝利益关系的项目，其它话才可信。

⬜ **待你确认**：中转站是否留存请求日志、留存多久。这一条必须在页面上写清楚。

### 线 3：不夸大

「价格很低」可以说（相对挨家注册的门槛，这是事实）。
❌ 不写「全网最低」「比官方便宜 X 折」这类可被证伪的话 ——
一旦被人比价打脸，损失的是整个项目的可信度。
具体价格让 bobdong.cn 自己承载，App 内不写死数字（数字会过期，代码改起来慢）。

---

## 4. 站外内容策略

Viva 本身就是最好的内容素材 —— 因为它有**别人没有的一手数据**。

### 4.1 已有的内容资产（复用现成文档）

仓库里这几篇是可以直接改写发出去的硬内容：

| 文档 | 可改写成 | 平台 |
|---|---|---|
| `02-豆包流式ASR接入.md` §10 十三个实战踩坑 | 《接豆包流式语音识别，我踩了 13 个坑》 | 掘金 / 少数派 / V2EX |
| `enable_nonstream` 开了热词失效（3/3 复现，与官方文档矛盾） | 《官方文档写错了：一个参数让热词全部失效》 | 掘金 / Twitter |
| `end_window_size` 1500ms 导致整段憋到最后 | 《为什么你的"边说边写"变成了"说完才出"》 | 少数派 |
| `04-技术实现方案.md` CGEvent 四大坑 | 《macOS 文本注入的四个坑》 | 掘金 |
| 蓝牙常驻预热把耳机拉进 HFP 通话模式 | 《语音软件为什么会让你的 AirPods 音质变差》 | 少数派 / 小红书 |

**这类内容的转化路径**：技术文章 → 「我做了个开源工具 Viva」→ README → 中转站。
三跳，但每一跳都是自然的，且第一跳带来的是**精准的开发者流量**。

### 4.2 内容原则

- **先给完再要** —— 文章本身必须能独立解决问题，不留钩子。留钩子的技术文章在中文开发者社区口碑极差。
- **中转站只出现在文末一行**，或者干脆只出现在 GitHub README 里。
- 一手实测数据是最大的差异化。首字 572–630ms、13 个踩坑、3/3 复现 ——
  这些数字别人抄不走，也正是它们让文章能被转载。

### 4.3 GitHub README

在「快速开始 → 可选：AI 润色」那一节，用**中立比价表**的形式呈现：

| 方案 | 要做几步 | 适合谁 |
|---|---|---|
| Ollama 本地 | 装 Ollama + pull 模型 | 完全不想联网、不想花钱 |
| 各家云厂商 | 注册 → 实名 → 充值 → 开通模型 → 建 Key → 查模型名 | 已经有账号的 |
| **Viva 中转站** | 拿 Key → 填进去 → 点「拉取模型」 | 想最快用上的 |

并在表下方一行注明：「中转站由本项目作者运营，属于本项目的收入来源。」
**自曝比被扒好一万倍。**

---

## 5. 定价与包装建议

⬜ 以下是建议，具体数字要你根据实际成本定。

### 5.1 给 Viva 用户一个专属入口

App 里的两个按钮都指向带来源标记的地址：

```
https://bobdong.cn/?from=viva
```

好处有三：**能算清 Viva 到底带来多少转化**、可以给这批人做专属优惠、
而且不需要在 App 里埋任何统计代码（见 §6）。

### 5.2 首单门槛压到最低

润色一次的成本极小（一两句话，轻量档模型，单次约几厘）。
所以**送一个够用一两个月的体验额度**，边际成本几乎为零，但能直接跨过"要先充值"这道心理坎。

建议：`?from=viva` 进来的用户送 ⬜ N 元体验额度，无需充值即可开通。
文案：「Viva 用户免费额度，够润色约 ⬜ 万字」——
**把额度翻译成用户听得懂的单位（字数），不要说 token。**

### 5.3 定价锚点

页面上把「挨家注册要做的事」和「中转站要做的事」并排放，
锚点是**时间成本**不是价格。价格战打不赢大厂，时间成本能。

---

## 6. 数据与埋点

### ⛔ 不要在 App 里加统计 SDK

Viva 是本地优先的开源工具，`README` 和设置页都写了「记录、配置全部保存在本机」。
加任何埋点都会被 diff 出来，得不偿失。

### ✅ 全部在中转站侧测量

| 指标 | 怎么拿 |
|---|---|
| Viva 带来的访问 | `?from=viva` 的 UA/来源统计 |
| 访问 → 注册 | 落地页转化率 |
| 注册 → 首次调用 | 首个 API 请求 |
| 是否来自 Viva 的调用 | ⬜ 建议 App 在请求里带 `User-Agent: Viva/0.3.0`（**只带版本号，不带任何用户标识**） |
| 留存 | 次月仍有调用的比例 |

**北极星指标**：`Viva 下载量 → 中转站首次调用` 的整体转化率。
先测出基线，再谈优化哪个转化位。

⬜ `User-Agent` 这条需要你确认要不要做 —— 它是本方案里唯一会往外发的新信息，
虽然只有版本号、不含任何用户标识，但仍应写进隐私说明里。

---

## 7. 落地排期

| 阶段 | 内容 | 状态 |
|---|---|---|
| **第一步** | 中转站预设 + 一键配置 + 自动拉模型 + 两处转化位 | ✅ 已完成 |
| **第一步（收尾）** | 隐私话术改成分层表述（§3 线 2） | ⬜ **必须在下次发版前做完** |
| **第一步（收尾）** | 确认 `https://bobdong.cn/v1` 是否是正确的 baseURL | ⬜ 待你确认 |
| **第二步** | README 比价表 + `?from=viva` 落地页 + 体验额度 | ⬜ |
| **第三步** | 润色失败兜底提示、引导第 4 步 | ⬜ |
| **持续** | 每两周发一篇实测向技术文章（素材见 §4.1） | ⬜ |

---

## 8. 待你确认的信息 ⬜

代码里的文案是按你的描述写的，这几条我没有事实依据，**发版前请核对**：

1. **baseURL** —— 我按 one-api / new-api 的惯例填了 `https://bobdong.cn/v1`。
   如果实际不是这个，改 `app/Sources/Viva/LLMProvider.swift` 里 `relayID` 那个预设的 `baseURL`。
2. **`GET /models` 是否开放** —— 「自动拉取模型列表」整个卖点依赖它。
   如果没开，这条得从文案里拿掉（拉不到会显示友好错误，不会崩，但卖点就空了）。
3. **是否支持 OpenAI Chat Completions 协议** —— 一键配置默认按这个填的。
4. **日志留存策略** —— §3 线 2 的隐私说明要写实话。
5. **体验额度** —— 给不给、给多少。
6. **实际价格** —— App 内没写死数字（故意的），但落地页需要。

---

## 9. 一句话总结

**把中转站放在用户已经卡住的地方，用「省掉几步」而不是「便宜多少」说话，
并且主动把利益关系和数据流向讲在前面。**

开源项目的商业化，可信度就是转化率本身。

