# 11 · 竞品功能调研(2026-07 全网扫描)

> 对照对象:Viva 0.11.0(已有:预设词库云同步、改词记忆、两段式润色、连续听写、
> 按 App Profile、dialog_ctx、粘贴上一段、preRoll 首字不丢、自动更新)。
> 结论先行:**下一个该做的是「改口自动纠正」**,其次是语音改写选中文本和 Snippets。

---

## 1. 竞品要点(2026-07 现状)

| 产品 | 值得注意的功能 | 对 Viva 的启示 |
|---|---|---|
| **Typeless** | ⭐ **改口自动纠正**:「明天上午9点…啊不对,下午3点」→ 直接输出改后的最终意思;去重复/去犹豫;语音编辑 | 六大 AI 输入法横测作者**付费的唯一理由**就是这个。中文口语「说错重说」极高频 |
| **Wispr Flow** | Command Mode(选中文本+说指令改写)、Whisper Mode(轻声识别,独家)、Snippets(说触发词→插整段文本)、词典自动学词 | 2026-05 **截屏上下文爆隐私丑闻**(Reddit 抓包→封号→CTO 道歉)。我们的 dialog_ctx 只发自家识别文本,是隐私安全的差异点,该写进营销 |
| **superwhisper** | Modes(模型+prompt+**按 App/网站自动激活**,v2.12 每模式独立快捷键)、Meeting 模式、**v2.13 加了 Claude Code/Open Code agent 集成** | 它都开始集成 Claude Code 了 —— 那正是我们用户群 |
| **VoiceInk**(开源 GPLv3,4.1k★) | Power Mode 到 **URL 级**(Gmail 里自动切邮件模式)、截屏+OCR 上下文(被评不可靠)、本地 whisper.cpp/Parakeet | URL 级 Profile 是我们 Profile 的自然下一步 |
| **闪电说** | 本地 ASR+云端 AI 混合、技能执行(Agent 化) | 社区正在用「闪电说+火山 Key」凑豆包 PC 输入法 —— 这个需求 Viva 就是现成答案 |
| **豆包输入法 PC** | **官方仍未上线**(2026-07) | 05 章判断的窗口期还在 |

开源侧(borrow 思路不 borrow 代码):Handy(14k★,Tauri/Rust 极简本地)、
OpenWhispr(MIT,AI agent+MCP server)、awesome-voice-typing(全平台索引)。
共同点:**本地模型(whisper.cpp/Parakeet)是开源阵营标配**,Viva 是纯云端。

## 2. 值得做的功能(按 价值/成本 排序)

### ⭐ P0:改口自动纠正(Typeless 的杀手锏)
用户说「周三交,呃不对,周五交」→ 上屏「周五交」。
**成本极低**:我们已有两段式润色管线,这本质是**默认 prompt 的升级**
(去犹豫、去重复、识别自我更正只保留最终意思)+ 文案上把它立成独立卖点。
与「绝不退格」不冲突 —— 润色模式本来就是松手后整段上屏。
不开润色的用户走服务端 `enable_ddc`(已开,只能去口水词,改不了口)。

### P1:Snippets(语音触发短语)
说「插入我的邮箱」→ 展开成完整邮箱。**replaceRules 已能覆盖 80%**
(from=触发词,to=长文本),要做的只是:词库页给个「短语」分区 + 引导文案。

### P1:语音改写选中文本(Command Mode)
选中一段 + 按住热键说「改得正式一点」→ 原地替换。中文侧仍空白(07 章 P2)。
成本中:读选区(AX API/⌘C)、改写走现有 LLM 管线、写回。

### P2:URL 级 Profile
Profile 已按 App 切,浏览器内再按网站切(AX 读地址栏)。VoiceInk 已验证需求。

### P2:会议/文件转写
拖入录音 → `volc.bigasr.auc_turbo`(0.8 元/小时)。superwhisper/MacWhisper 的场,
我们有成本优势,但离「输入」主线远,排后。

### P3:本地离线后备
开源阵营标配(whisper.cpp)。价值真实(断网/隐私敏感者),但破坏「无第三方依赖」
且中文效果不如豆包 —— 等有明确用户呼声再动。

### 明确不做
- **截屏上下文**:Wispr 的隐私丑闻就是前车之鉴;dialog_ctx 已拿到大半收益且隐私干净。
- **Agent 化/技能执行**:闪电说已占位,超出输入工具定位(07 章原判断维持)。

## 3. 营销层可立即行动

1. 「豆包输入法电脑版还没出?Viva 就是」—— 社区正拿闪电说+火山 Key 凑,我们开箱即用。
2. 隐私对比页:Wispr 截屏上传 vs Viva 只发你自己账号的音频+自家识别文本。
3. superwhisper 都在集成 Claude Code —— 我们的终端/IDE Profile + 技术词库就是给这群人的,该讲出来。

## 4. 来源

Wispr docs/评测(droidcrunch、tldv、metawhisp)、superwhisper changelog、
VoiceInk GitHub/tryvoiceink、Typeless 虎嗅/少数派/App Store、
腾讯新闻六大 AI 输入法横测、awesome-voice-typing、OpenWhispr/Handy GitHub。
