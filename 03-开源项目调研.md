# 开源项目调研

> 调研时间：2026-07。star 数为 2026-07-25 经 shields.io 实时接口核对。
> ⚠️ **本章在深度调研后做过一次重大修正** —— 初版曾判断「对接豆包流式的开源项目很少、实时上屏是生态空白」，**该结论错误**。修正见下。

---

## 0. 一句话结论（修正版）

**这条赛道已经很拥挤，而且有直接对标物。**

1. **对接火山/豆包流式 ASR 的开源项目已经很多**，且**全部收敛到 `bigmodel_async` 端点**。GitHub 代码搜索 `"sauc/bigmodel"` 有 **1000+ 命中**。
2. **存在一个与目标高度重合的项目**：[`joewongjc/type4me`](https://github.com/joewongjc/type4me) —— macOS 语音输入工具，**1366 star / MIT**，已接火山流式 + 13 家 LLM 后处理 + 热词 + 全本地历史。作者自我定位「Typeless 平替」。
3. **还有一个定位几乎完全重合的**：[`mylxsw/typeflux`](https://github.com/mylxsw/typeflux) —— 菜单栏形态（非输入法）、V0.2.0 已做实时流式、本地模型可离线、自称「桌面端语音 Agent」。**它占住了我原本规划的「非 IME 形态」+「隐私自持」两个差异点。**
4. **但「真·增量上屏」确实仍是空白** —— 已实测确认：typeflux 的 `TextInjection/AXTextInjector.swift` 只提供 `insert()` / `replaceSelection()` 整段接口，**不做 partial 替换跟踪，不支持增量流式注入**。一个把协议写到 1149 行含单测的项目都没做增量注入，说明非 IME 路线上它确实没有安全解。

> **修正后的判断**：
> 空白不在「对接豆包」，也不在「非 IME 形态」——那两块都已被占。
> 真正的空白只剩一条极窄的缝：**在已上屏文本上做安全的增量改写**（而这只有 InputMethodKit 能干净实现）。
> 立项前必须先接受这个现实，再决定差异化打法（见 [05 章](05-产品设计方案.md)）。

---

## 1. 已对接火山/豆包流式的开源项目 ⭐ 最相关

| 项目 | Star | 语言 | 协议路线 | 关键文件 | 价值 |
|---|---|---|---|---|---|
| **[joewongjc/type4me](https://github.com/joewongjc/type4me)** | **1366** / MIT | Swift | 无序号 | `Type4Me/ASR/VolcASRClient.swift`（443 行） | ⭐⭐⭐ **最直接的对标物**。端点写死 `bigmodel_async`；鉴权用 `X-Api-App-Key`/`X-Api-Access-Key`/`X-Api-Resource-Id`/**`X-Api-Connect-Id`**（注意用的是 Connect-Id 而非 Request-Id） |
| **[mylxsw/typeflux](https://github.com/mylxsw/typeflux)** | 322 / — | Swift | 无序号 | `Sources/Typeflux/STT/DoubaoRealtimeTranscriber.swift`（**1149 行，含协议单测**） | ⭐⭐⭐ **最完整的 Swift 参考实现**：header 编解码、`buildClientRequest`、`encodeAudioPacket`、`decodeServerResponse`、gzip、**连接预热**与重试俱全 |
| **[Open-Less/openless](https://github.com/Open-Less/openless)** | **2849** / MIT | Rust | **正序号** | `app/src-tauri/src/asr/volcengine.rs`（764 行）+ `asr/frame.rs` | ⭐⭐⭐ 文件头注释写明是 Swift 版移植，**保留了大量实战踩坑注释**。`insertion.rs` 做「流式逐字写入光标」，是目前最接近目标的非输入法实现 |
| [shenjingnan/univoice](https://github.com/shenjingnan/univoice) | — | TypeScript | 正序号 + gzip | `src/asr/protocols/sauc.ts`（380 行） | 含完整 `SAUCErrCode` 枚举 |
| [shenjingnan/univoice-rs](https://github.com/shenjingnan/univoice-rs) | — | Rust | — | `src/asr/protocol/sauc.ts` | |
| [xinnan-tech/xiaozhi-esp32-server](https://github.com/xinnan-tech/xiaozhi-esp32-server) | — | Python | 全程 gzip | `core/providers/asr/doubao_stream.py`（430 行） | 服务端场景 |
| [Chauncy-Guo/doubao-asr](https://github.com/Chauncy-Guo/doubao-asr) | — | Python | 无序号 | 单文件 | 注释注明协议参考自 type4me |
| [that-yolanda/voicepaste](https://github.com/that-yolanda/voicepaste) | — | Rust | — | `src-tauri/src/asr/doubao.rs` | |
| [233stone/vocotype-cli](https://github.com/233stone/vocotype-cli) | ~857 | Python | — | — | 默认后端 FunASR，火山为可选 |
| [yeyitech/voicevibe](https://github.com/yeyitech/voicevibe) | 极少 | Electron | — | — | 多 Provider 抽象、热词合并 |
| 其它同类 | — | — | — | — | `joe223/AriaType`、`crosswk/SayIt`、`yyyzl/push-2-talk`、`tover0314-w/opentypeless`、`yizhisec/fcitx5-anytalk`（**其 `docs/doubao-asr-api.md` 是一份完整的协议中文镜像**） |

> ⚠️ **一个重要事实**：上述所有实现用的都是 **1.0 的 `volc.bigasr.sauc.*`** Resource ID。
> **没有任何开源实现验证过 2.0 的 `volc.seedasr.sauc.*`** —— 2.0 是否同样支持 `bigmodel_async` / `show_utterances` / `definite` / 热词直传，**必须自己实测**。这是一个真实的未知项。

---

## 2. 通用语音输入工具（架构参考）

| 项目 | Star | 平台 | 技术栈 | 许可证 | 借鉴点 |
|---|---|---|---|---|---|
| **[cjpais/Handy](https://github.com/cjpais/Handy)** | **27k** | 跨平台 | Rust + Tauri | **MIT** | ⭐ **这个领域最大的开源参照物**。`enigo` + clipboard 注入链路值得精读 |
| **[Beingpax/VoiceInk](https://github.com/Beingpax/VoiceInk)** | **5.7k** | macOS | Swift + WhisperKit | ⚠️ **GPL-3.0** | 设计可参考（`PasteMethod{standard, appleScript}` 双通道、Power Mode 按 App 切配置），但**代码不能抄** —— copyleft 会传染到闭源产品 |
| [HaujetZhao/CapsWriter-Offline](https://github.com/HaujetZhao/CapsWriter-Offline) | ~6.4k | **仅 Win** | Python + ONNX | — | 客户端/服务端分离；音素模糊匹配热词 |
| [evoleinik/fnkey](https://github.com/evoleinik/fnkey) | ~46 | macOS | Rust | — | Deepgram 流式推流 |
| [OpenWhispr/openwhispr](https://github.com/OpenWhispr/openwhispr) | — | 跨平台 | Whisper/Parakeet | — | 本地 + 云端双后端 |
| [primaprashant/awesome-voice-typing](https://github.com/primaprashant/awesome-voice-typing) | — | — | — | — | ⭐ 最全清单，明确标注每个项目是否流式 |

> **许可证是必须先拍板的事**：可安全取用代码的是 **type4me / Handy / openless（均 MIT）**；VoiceInk 是 GPL-3.0，**只能看设计不能抄代码**。

---

## 3. 技术积木

| 项目 | 用途 |
|---|---|
| [snakers4/silero-vad](https://github.com/snakers4/silero-vad) | VAD 首选（ONNX ~1.8MB）。⚠️ 用法见 [02 章](02-豆包流式ASR接入.md) 的「包级门控会污染流式 cache」警告 |
| [ufal/whisper_streaming](https://github.com/ufal/whisper_streaming) | LocalAgreement-2 稳定前缀算法出处（用豆包的 `definite` 可省） |
| [modelscope/FunASR](https://github.com/modelscope/FunASR) | 本地后备后端 |
| [k2-fsa/sherpa-onnx](https://github.com/k2-fsa/sherpa-onnx) | 端侧推理引擎（type4me 本地版的底座） |
| [enigo-rs/enigo](https://github.com/enigo-rs/enigo) | ⭐ Rust 跨平台输入模拟。**其 macOS 实现里的 workaround 就是一份 CGEvent 踩坑清单**（20 字符截断 #68、`\n\r\t` 前置零宽空格 #260、显式清空修饰键 flags） |

---

## 4. 输入法框架（IMK 路线参考）

| 项目 | 说明 |
|---|---|
| [vChewing/vChewing-macOS](https://github.com/vChewing/vChewing-macOS) | ⭐ 纯 Swift macOS 输入法最佳教材 |
| [Shiki Suen · macOS 输入法开发指南 2026](https://shikisuen.medium.com/macos-input-method-development-guidelines-for-2026-5123461fa53b) | ⭐⭐ **必读**。明确了两条硬约束：① 所有 client 调用必须在 MainActor；② `setMarkedText` 与 `insertText` 是唯二**不能异步派发**的方法 |
| [fcitx/mozc](https://github.com/fcitx/mozc)（macOS 部分） | 工业级 `IMKInputController` 范本 |
| [rime/librime](https://github.com/rime/librime) + [Squirrel](https://github.com/rime/squirrel) | 想做「语音 + 拼音」双模输入法时的现成底座 |

**IMK 可行性的关键证据（好消息）**：

> `IMKInputController` 的 `candidateSelected:` 由**候选窗鼠标点选**触发，SDK 头文件注释明确写「典型响应是调用 client 的 insertText」。
> —— 这证明 **`client()` 代理对象可以在 `handle(event:)` 之外的路径被合法调用**，即「音频事件驱动上屏」在 IMK 的设计里是成立的。`updateComposition` 也是主动刷新 marked text 的入口。

**商业先例**：搜狗输入法 Mac 版把语音输入内置在输入法里，双击 Option 触发、支持「按着说」、宣称「所说即所见」实时上屏。**IME 内由音频驱动异步上屏在产品上已被验证可行。**

---

## 5. 修正后的竞争格局

```
                     非流式(录完再转)              真流式(边说边出)
                 ┌──────────────────────┬──────────────────────────────┐
  松手/逐句       │ Handy 27k            │ type4me 1.4k ⭐直接对标        │
  整段注入        │ VoiceInk 5.7k        │ typeflux 322 ⭐形态重合         │
                 │ CapsWriter 6.4k      │ openless 2.8k                 │
                 │ OpenWhispr …(20+)    │ LazyTyper / 闪电说（闭源）      │
                 ├──────────────────────┼──────────────────────────────┤
  真·增量改写     │      （无意义）        │  仍是空白，但只有 IMK 能做       │
  已上屏文本      │                      │  商业先例：搜狗Mac、豆包输入法    │
                 └──────────────────────┴──────────────────────────────┘
```

**可直接复用的成果**：

| 需求 | 抄谁 | 许可证 |
|---|---|---|
| 豆包 sauc 协议（Swift） | `typeflux` 的 `DoubaoRealtimeTranscriber.swift` + 单测 | 需确认 |
| 豆包 sauc 协议（Rust，含踩坑注释） | `openless` 的 `volcengine.rs` | MIT |
| 协议中文文档镜像 | `fcitx5-anytalk/docs/doubao-asr-api.md` | — |
| 文本注入策略链 | `type4me` / `Handy` | MIT |
| CGEvent 踩坑 workaround | `enigo` 的 `macos_impl.rs` | MIT |
| 剪贴板恢复延迟常量 | `typeflux`（普通 150ms / 慢终端 1.5s） | 需确认 |
| IMK 实现 | `vChewing-macOS` + Shiki Suen 2026 指南 | — |

**必须自己造的（真正剩下的空白）**：

1. **增量上屏**：partial 反复重写已上屏文本 —— 非 IME 路线无安全解，IMK 路线无开源先例
2. **可编程热词**：按前台 App / 当前 Git 仓库动态生成 `corpus.context`
3. **首字环形预缓冲**：热键按下前回溯 300~500 ms —— **豆包官方 Mac 版自己都有漏字问题**

---

## 6. 调研完整性说明

本章基于一次 6 维度并行深挖 + 交叉验证工作流，其中：

- ✅ **protocol-impl**（协议实现与踩坑）、**macos-injection**（上屏方案）、**competitors**（竞品）三个维度完成并通过核查（均为 `PARTIALLY_CONFIRMED`，修正项已并入正文）
- ❌ **oss-projects**、**stabilization**（文本稳定化算法）、**audio-vad**（音频与 VAD）三个维度的 agent 因连接中断失败

**因此以下内容尚未经过深度核查，属于已知缺口**：

| 缺口 | 现状 | 影响 |
|---|---|---|
| 音频采集链路（AVAudioConverter 抗混叠、蓝牙切换、环形预缓冲实现） | [01 章](01-语音输入法实现原理.md)/[04 章](04-技术实现方案.md) 的内容基于通用工程经验，未经二次验证 | 中 |
| 文本稳定化算法细节（LocalAgreement-n 变体、中文标点抖动处理） | 同上 | 低（豆包有 `definite`，可绕开） |
| 端到端延迟的**一手实测数字** | **完全缺失**，全文所有延迟数字都是估算 | ⚠️ **高** —— 无法定义验收标准 |

> **`awesome-voice-typing` 清单里「仅 2 个项目支持流式」这个说法应当作废** —— 该清单主要收录英文生态的 Whisper 系工具，完全没有覆盖中文圈已接云端流式 ASR 的这一批项目。用它论证「流式很稀有」是取样偏差。
