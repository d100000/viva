# 技术实现方案

> 目标平台：**macOS 优先**（你当前环境 darwin 25.5 / macOS 15+），架构预留跨平台。
> 核心目标：对接豆包流式 ASR，实现**边说话边打字**。

---

## 1. 关键决策：两种形态，分两步走

「边说话边打字」有两种落地形态，**能力和成本差一个数量级**：

| | **形态 A：全局热键工具** | **形态 B：真·输入法（IMK）** |
|---|---|---|
| 用户怎么用 | 在任意 App 按住 Fn 说话 | 切换到「XX 语音输入法」后说话 |
| 上屏机制 | 模拟键入 / 剪贴板粘贴 | 输入法预编辑区（marked text） |
| 中间结果显示 | **悬浮 HUD 窗口**（不进输入框） | **光标处带下划线的预编辑文本** ⭐ |
| 真·逐字上屏 | ❌ 做不到（改写会破坏输入框） | ✅ 原生支持 |
| 上屏粒度 | 逐句（definite） | 逐字 partial + 逐句 commit |
| App 兼容性 | 好（剪贴板兜底几乎无死角） | 极好（所有正经输入框） |
| 权限 | 麦克风 + 辅助功能 + 输入监控 | 麦克风（无需辅助功能） |
| 开发成本 | **2~3 周** | **+2~3 周**，且有**可行性风险** |
| 开源先例 | 大量 | **无** |

### 推荐路线

> **先做 A 打透体验，再做 B 突破形态。**
>
> - **A 阶段**：中间结果显示在**紧贴光标的悬浮 HUD**里（实时逐字滚动），`definite` 分句时才真正写入输入框。
>   把 `end_window_size` 调到 500~600 ms，用户体感是「说一句出一句」，配合 HUD 的逐字预览，**已经非常接近「边说边打字」**，而且零兼容性风险。
> - **B 阶段**：验证 IMK 可行性后，把同一套 Core 复用到输入法 App 上，实现真正的光标处逐字上屏。

**为什么不直接上 B**：见 §6 的可行性风险 —— IMK 输入法能否由「音频事件」而非「按键事件」异步驱动上屏，是一个**未经开源生态验证**的问题。不该把整个项目押在上面。

---

## 2. 总体架构

```
┌──────────────────────────────────────────────────────────────┐
│                        UI 层（可替换）                        │
│  ┌────────────┐  ┌──────────────┐  ┌──────────────────────┐  │
│  │ 菜单栏图标  │  │ 悬浮 HUD      │  │ 设置窗口 / 热词管理   │  │
│  │ MenuBarApp │  │ (光标跟随)    │  │ SwiftUI              │  │
│  └────────────┘  └──────────────┘  └──────────────────────┘  │
└───────────────────────────┬──────────────────────────────────┘
                            │  (事件/状态订阅)
┌───────────────────────────▼──────────────────────────────────┐
│                  VoiceCore（可复用内核）                       │
│                                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────────┐  │
│  │ Hotkey   │→ │ Session  │→ │ Audio    │→ │ VAD         │  │
│  │ Manager  │  │StateMach.│  │ Capture  │  │ (Silero)    │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────┬──────┘  │
│                                                    │         │
│  ┌──────────────────────────────────────────┐     │         │
│  │  ASRClient (protocol)                     │◄────┘         │
│  │   ├─ DoubaoStreamingASR  ⭐ 主实现         │               │
│  │   ├─ LocalWhisperASR     （隐私模式备选）  │               │
│  │   └─ MockASR             （测试用）        │               │
│  └───────────────────┬──────────────────────┘               │
│                      │ partial / definite                    │
│  ┌───────────────────▼──────────────────────┐               │
│  │  TextStabilizer  （稳定化 + diff）        │               │
│  └───────────────────┬──────────────────────┘               │
│                      │                                       │
│  ┌───────────────────▼──────────────────────┐               │
│  │  PostProcessor（热词替换 / LLM 润色）      │               │
│  └───────────────────┬──────────────────────┘               │
│                      │                                       │
│  ┌───────────────────▼──────────────────────┐               │
│  │  TextInjector (protocol)                  │               │
│  │   ├─ KeystrokeInjector  (CGEvent)         │               │
│  │   ├─ ClipboardInjector  (⌘V, 长文本)      │               │
│  │   ├─ AXInjector         (可选优化)         │               │
│  │   └─ IMKInjector        (形态 B)  ⭐       │               │
│  └──────────────────────────────────────────┘               │
└──────────────────────────────────────────────────────────────┘
```

**设计要点：`VoiceCore` 与 UI/上屏方式完全解耦。**
这样形态 A（菜单栏 App）和形态 B（输入法 App）可以共享同一个内核 —— 只是换掉 `TextInjector` 实现和 UI 外壳。

---

## 3. 技术选型

| 方案 | 优势 | 劣势 | 建议 |
|---|---|---|---|
| **Swift + AppKit/SwiftUI** ⭐ | macOS 原生能力全开放（IMK 只能用 Swift/ObjC）；`AVAudioEngine` 最省事；分发体验最好；无运行时依赖 | 只能 macOS | ⭐⭐⭐ **推荐**。形态 B（IMK）**只能**走这条 |
| Rust + Tauri | 跨平台；Handy/FnKey 已验证；Web UI 开发快 | IMK 需要写 ObjC 桥；音频/权限都要绑定 | ⭐⭐ 如果一开始就要 Windows |
| Python | 原型最快（1 天能跑通协议） | 分发困难（打包体积大）、热键/上屏库在 macOS 支持差（CapsWriter 就卡在这） | ⭐ **仅用于 M0 协议验证** |
| Electron | UI 开发快，VoiceVibe 已验证 | 体积大、内存高、原生能力受限、IMK 无解 | ❌ 不推荐 |

> **结论：Python 打前站（验证协议）→ Swift 做产品。**
> 先用 30 行 Python 把豆包协议跑通（见 02 章代码），确认账号/参数/延迟符合预期，再用 Swift 正式实现。这样能把「协议调试」和「App 工程」两个风险源分开。

---

## 4. 核心模块设计

### 4.1 AudioCapture

```swift
final class AudioCapture {
    private let engine = AVAudioEngine()
    private var converter: AVAudioConverter?
    private let targetFormat = AVAudioFormat(
        commonFormat: .pcmFormatInt16, sampleRate: 16000,
        channels: 1, interleaved: true)!

    var onChunk: ((Data) -> Void)?     // 每 200ms 回调一次
    private var buffer = Data()
    private let chunkBytes = 16000 * 2 * 200 / 1000   // 200ms @16k/16bit = 6400 B

    /// ⭐ 预热：App 启动就调用，避免热键按下时的冷启动丢字
    func prewarm() throws {
        let input = engine.inputNode
        let inputFormat = input.outputFormat(forBus: 0)   // 通常 48kHz f32
        converter = AVAudioConverter(from: inputFormat, to: targetFormat)

        input.installTap(onBus: 0, bufferSize: 1024, format: inputFormat) { [weak self] buf, _ in
            guard let self, self.isCapturing else { return }   // 未录音时直接丢弃
            self.process(buf)
        }
        try engine.start()
    }

    private(set) var isCapturing = false
    func start() { isCapturing = true; buffer.removeAll(keepingCapacity: true) }
    func stop()  { isCapturing = false; flushRemaining() }

    private func process(_ buf: AVAudioPCMBuffer) {
        // 1. 重采样 48k→16k + 降混 + f32→s16
        // 2. 追加到 buffer
        // 3. 每满 chunkBytes 就 onChunk?(chunk)
    }
}
```

**要点**：
- `prewarm()` 在 App 启动时调用，引擎常驻空转，`isCapturing` 开关只控制「是否把数据往外送」。**这是消除首字丢失的关键。**
- 监听 `AVAudioEngineConfigurationChangeNotification` 处理设备热插拔。
- 检测蓝牙耳机（`AVAudioSession` 在 macOS 上不可用，需查 `AudioObjectGetPropertyData` 的 transport type），命中时提示用户或建议切换设备。

### 4.2 DoubaoStreamingASR

```swift
protocol ASRClient: AnyObject {
    func connect() async throws                    // 支持预热
    func start(config: ASRConfig) async throws     // 发 full client request
    func send(audio: Data, isLast: Bool) async throws
    var onResult: ((ASRResult) -> Void)? { get set }
    func cancel()
}

struct ASRResult {
    let definiteText: String     // 本次新增的已定稿文本
    let partialText: String      // 未定稿预览文本
    let isFinal: Bool
}
```

实现要点（协议细节见 [02 章](02-豆包流式ASR接入.md)）：

1. **连接预热**：`connect()` 与 `start()` 分离。热键按下前就建好 WS 连接（但**不发首包**，避免触发「等包超时」计费与 `45000081`）。松手/超时后关闭。
   - 更稳的做法：按下热键的瞬间同时发起建连和启动采集，前 200 ms 的音频先进缓冲，连上后一次性补发。**这样既不挂空连接，也不丢字。**
2. **发送队列**：音频包进 `AsyncStream`，单一消费者顺序发送，网络阻塞时丢最老的包（而不是阻塞采集线程）。
3. **末包**：松手时用 `flags = 0b0010` 发最后一包，然后**等待最终结果，带 3 秒超时兜底**。
4. **日志**：必存 `X-Tt-Logid`。

### 4.3 TextStabilizer

```swift
final class TextStabilizer {
    private var committedCount = 0     // 已提交的分句数（result_type=full 时做游标）
    private(set) var committed = ""
    private(set) var preview = ""

    /// 返回：(需要新提交的文本, 需要预览的文本)
    func consume(_ utterances: [Utterance], resultType: ResultType) -> (commit: String, preview: String) {
        var newCommit = ""
        var pending = ""

        switch resultType {
        case .single:
            // 增量模式：直接分类
            for u in utterances {
                if u.definite { newCommit += u.text } else { pending += u.text }
            }
        case .full:
            // 全量模式：用游标跳过已提交的分句
            let definites = utterances.filter(\.definite)
            if definites.count > committedCount {
                newCommit = definites[committedCount...].map(\.text).joined()
                committedCount = definites.count
            }
            pending = utterances.filter { !$0.definite }.map(\.text).joined()
        }

        committed += newCommit
        preview = pending
        return (newCommit, pending)
    }
}

/// 需要替换已上屏文本时用（形态 B 的预编辑区 / 形态 A 的退格重写）
func computeEdit(from prev: String, to next: String) -> (backspaces: Int, insert: String) {
    // ⚠️ 必须按 Character（Swift 的 Character = Unicode 字素簇）计数，不能用 utf16.count
    let p = Array(prev), n = Array(next)
    var i = 0
    while i < p.count, i < n.count, p[i] == n[i] { i += 1 }
    return (p.count - i, String(n[i...]))
}
```

### 4.4 TextInjector：四条路线与兼容性

```swift
protocol TextInjector {
    func commit(_ text: String) throws        // 提交定稿文本
    func setPreedit(_ text: String) throws    // 更新预编辑（仅 IMK 支持）
    func clearPreedit() throws
}
```

> ⚠️ **这是整个项目最大的单点风险。协议是最简单的部分，注入才是。**
> 而且**失败往往是静默的** —— AX 写入返回 `success` 但 UI 没变、Secure Input 下事件全灭且无任何错误返回。

**兼容性矩阵**（已核实，含具体失败原因）：

| 目标 App | CGEvent 模拟键入 | 剪贴板 ⌘V | 辅助功能 (AX) | 备注 |
|---|---|---|---|---|
| 原生 Cocoa（备忘录/TextEdit/邮件/Pages） | ✅ | ✅ | ✅ | 唯一 AX 稳定的一类 |
| Chrome / Safari 输入框 | ✅ | ✅ | ❌ | Chrome 需先设 `AXEnhancedUserInterface=true` 才拿得到可编辑元素；`kAXSelectedTextAttribute` 在 Mail/Chrome/Firefox 上不工作；Web 输入框大量是 `kAXComboBoxRole`，设置该属性**无效果也无错误** |
| **Electron**（VSCode/Cursor/Slack/飞书/Discord/Obsidian） | ✅ | ✅ | ❌ **永久不可用** | 需设 `AXManualAccessibility=true`；选区常无条件返回 `{location:0,length:0}` 无法定位光标。[electron#36337](https://github.com/electron/electron/issues/36337) 已 **Closed as not planned**（官方明确不修） |
| 终端（Terminal/iTerm2/Ghostty/Warp） | ⚠️ | ⚠️ | ❌ | Secure Keyboard Entry 开启时**全灭**；bracketed paste 会把 ⌘V 包裹 `ESC[200~/201~`；vim 特定模式按字节解析会错 |
| Microsoft Office | ⚠️ 偶发丢字 | ✅ | ❌ 只读 | 自绘文本引擎 |
| Xcode | ⚠️ | ✅ | ❌ 只读 | **自动补全会吃掉/改写注入的字符**，必须禁用逐字流式 |
| Figma / Notion 等 Canvas | ✅ | ✅ | ❌ | |

**结论：AX 直写不能当主路径。** 但保留它的两个**只读**用途：
1. 读 `kAXFocusedUIElement` 判断「当前是否真的聚焦在可编辑文本框」
2. 读 `kAXSelectedTextRange` 拿插入点位置来**定位 HUD 悬浮窗**

**三级策略链**（照抄 `type4me` / `Handy` / `openless` 的成熟做法）：

```swift
final class InjectionChain: TextInjector {
    func commit(_ text: String) throws {
        // ⓪ 前置检查：Secure Input 下一切都会静默失败
        guard !IsSecureEventInputEnabled() else {
            copyToClipboard(text)
            hud.warn("当前 App 开启了安全键盘输入，已复制到剪贴板，请按 ⌘V")
            return
        }

        // ① 首选 CGEvent 分块注入：不改剪贴板、不占 undo 栈、支持逐段流式
        if text.count <= 20, (try? KeystrokeInjector().commit(text)) != nil { return }

        // ② 降级剪贴板 + ⌘V：长文本或 ① 失败
        if (try? ClipboardInjector().commit(text)) != nil { return }

        // ③ 再降级 AppleScript（需要额外的「自动化」TCC 授权，是独立的第二个弹窗）
        try AppleScriptInjector().commit(text)
    }
}
```

> `VoiceInk` 内置 `PasteMethod{standard, appleScript}` 双通道枚举 —— 说明单靠 CGEvent 模拟 ⌘V 在部分 App 确实会失败，AppleScript 兜底是必要的。
> ⚠️ `VoiceInk` 是 **GPL-3.0**，只能参考设计不能抄代码；`type4me`/`Handy`/`openless` 是 MIT，可安全取用。

**必须内置按 bundleId 的兼容性策略表**，并允许用户按 App 手动覆盖（`VoiceInk` 的 Power Mode 形态）。

**`ClipboardInjector` 必须做的事**：

```swift
func commit(_ text: String) throws {
    let pb = NSPasteboard.general
    let saved = pb.pasteboardItems?.compactMap { item -> [NSPasteboard.PasteboardType: Data] in
        var d: [NSPasteboard.PasteboardType: Data] = [:]
        for t in item.types { d[t] = item.data(forType: t) }
        return d
    }                                       // 1. 备份原剪贴板（含所有类型）

    pb.clearContents(); pb.setString(text, forType: .string)
    postCmdV()                              // 2. 模拟 ⌘V

    DispatchQueue.main.asyncAfter(deadline: .now() + 0.25) {
        restore(saved)                      // 3. 延迟恢复（必须给目标 App 读取时间）
    }
}
```
**剪贴板方案的三个副作用与解法**：

| 副作用 | 解法 |
|---|---|
| 污染用户剪贴板与剪贴板管理器历史 | ⭐ 写入时同时声明 `org.nspasteboard.TransientType` 与 `org.nspasteboard.AutoGeneratedType` —— **Maccy 等主流剪贴板管理器默认忽略这些类型**（见 [nspasteboard.org](https://nspasteboard.org/)） |
| 每次 paste 是一个独立 undo step，流式频繁 paste 会**打爆撤销栈** | 只在 definite 时提交，不要高频 paste |
| 恢复旧剪贴板存在**竞态**（恢复太早会粘出旧内容） | 记录 `changeCount`，粘贴后等 changeCount 变化再恢复 |

> ⚠️ **恢复延迟的实测常量**（来自 `typeflux` 源码）：
> - 普通场景：**150 ms**
> - **未验证消费的慢终端（iTerm2 / Warp 的 bracketed paste）：1.5 秒**
>
> 250 ms 对慢终端是不够的。必须按目标 App 分档。

**`KeystrokeInjector` 的四个坑**（全部来自 `enigo` 的 macOS 实现 workaround）：

```swift
func typeUnicode(_ text: String) {
    let src = CGEventSource(stateID: .privateState)
    for chunk in text.chunkedByGraphemes(max: 20) {   // 坑① 20 字符截断
        let e = CGEvent(keyboardEventSource: src, virtualKey: 0, keyDown: true)!
        e.flags = []                                   // 坑③ 必须显式清空修饰键
        var units = Array(chunk.utf16)
        e.keyboardSetUnicodeString(stringLength: units.count, unicodeString: &units)
        e.post(tap: .cgSessionEventTap)
        // keyUp 同理
        usleep(2000)                                   // 坑④ 不加间隔会丢字
    }
}
```

| # | 坑 | 说明 |
|---|---|---|
| ① | **20 字符静默截断** | 一次调用超过 **20 个 UTF-16 单元**会被静默截断（`enigo` issue #68）。中文一字 = 1 个单元，但 **emoji/生僻字是代理对，分块时不能在代理对中间切断**，否则输出乱码 → 必须按字素簇分块 |
| ② | **`\n` `\r` `\t` 开头的块静默失败** | 整块什么都不输出（`enigo` issue #260）。`enigo` 的解法是前置零宽空格 `U+200B`，但**那个字符会真的留在用户文本里** → 更稳的做法是**换行单独用 keyCode 36（Return）发**，Tab 用真实 Tab 键码 |
| ③ | **被按住的修饰键污染注入** | 「按住说话」时用户正按着右 ⌘，注入事件会继承这个 flag → 变成 **⌘+文字 = 触发快捷键**。必须 `e.flags = []`，且 CGEventTap 要吞掉 `flagsChanged` 让目标 App 认为修饰键从未按下 |
| ④ | **注入过快丢字符** | `espanso` 实测：macOS 上 `inject_delay` 参数被忽略，但 `backspace_delay` 生效；Electron / 终端 / 自定义输入处理的 App 经常收不全模拟按键 |

**⚠️ 关于「退格重写」的最终裁定**：

> **MVP 严禁用 Backspace × N 做回改，只允许追加。**
>
> 风险不可接受：你不知道这期间用户有没有自己动过光标、点了别处、App 做了自动补全或自动大写、输入法把字符重组了。**一旦 N 算错就会删掉用户自己的文字，这是不可逆的破坏性操作。** 且 Electron/终端里按键可能丢帧，删除数量与实际不符。
>
> **已实测佐证**：`typeflux` 的 `AXTextInjector.swift` 只提供 `insert()` / `replaceSelection()` 整段接口，**不做任何 partial 替换跟踪**。一个把协议写到 1149 行含单测的项目都没做增量注入 —— 说明非 IME 路线上确实没有安全解。

### 4.5 HotkeyManager

**⚠️ 默认热键用右 Command（keyCode 54），不要用 Fn。** 理由见 [05 章](05-产品设计方案.md) §5.1：Fn 已被微信输入法与豆包输入法抢占，且大量第三方键盘根本不上报 Fn。

**键码参考**（与 Apple `HIToolbox/Events.h` 一致）：`Fn=63(0x3F)`、`右⌘=54(0x36)`、`左⌘=55(0x37)`、`左⌥=58(0x3A)`、`右⌥=61(0x3D)`

```swift
// 单修饰键"按住说话"只能用 CGEventTap 监听 flagsChanged
// （Carbon RegisterEventHotKey 要求必须有非修饰主键；NSEvent 全局 monitor 只能观察不能消费）
let tap = CGEvent.tapCreate(
    tap: .cgSessionEventTap, place: .headInsertEventTap,
    options: .defaultTap,        // 要吞事件必须 defaultTap（触发"辅助功能"权限）
                                 // .listenOnly 只能观察（触发"输入监控"权限）
    eventsOfInterest: CGEventMask(1 << CGEventType.flagsChanged.rawValue),
    callback: { _, type, event, _ in
        // ⭐ 必须处理：系统会静默禁用 tap
        if type == .tapDisabledByTimeout || type == .tapDisabledByUserInput {
            CGEvent.tapEnable(tap: theTap, enable: true)
            return nil
        }
        // 状态机：仅在"纯单键按住且持续 > ~150ms"时才判定为按住说话并吞掉事件
        return Unmanaged.passUnretained(event)
    }, userInfo: nil)
```

**四个必须处理的问题**：

1. ⭐ **tap 会被系统静默禁用** —— 这是生产环境头号坑。系统在超时或用户输入时会投递 `kCGEventTapDisabledByTimeout` / `ByUserInput` 并关掉 tap，表现为「热键运行一段时间后无声失灵」。
   > 原则：**"non-nil tap is not a healthy tap"** —— 必须运行时周期性校验 `tapIsEnabled`，而不是只在安装时判断创建成功。

2. ⭐ **TCC 授权绑定代码签名身份** —— 重新签名 / 改安装路径 / macOS 小版本升级都会让授权失效**且不再弹窗**，表现同样是「tap 创建成功但事件永不触发」。
   > 引导文案必须写「请先**移除**再重新添加」—— 重签名后仅重新勾选往往无效。
   > 签名 identifier 与安装路径一经确定**不要再变更**。

3. **吞事件的代价** —— 用 `.defaultTap` 吞掉右 ⌘ 的 `flagsChanged` 后，用户在按住期间的 ⌘C/⌘V 会失效。
   > 折中：仅在确认为「纯单键按住超过阈值」后才转入吞事件状态，短按放行给系统。

4. **Fn 的额外问题** —— 短按被系统占用（切输入法/表情/听写），第三方 tap 拿不到也压不住；工程上只能引导用户把系统设置里 `AppleFnUsageType` 改为「不执行任何操作」。**这也是不默认用 Fn 的原因之一。**

---

## 5. 安全设计：API Key 不能放客户端

**问题**：火山的 App Key / Access Token 一旦打进客户端 App，可被逆向提取 → 别人拿你的额度刷，账单是你的。

| 场景 | 方案 |
|---|---|
| **个人自用工具** | 存 **macOS Keychain**，用户自己填自己的 Key。零成本，够用 |
| **分发给他人 / 商业产品** | ⭐ 必须走**自建中转**：客户端 → 你的服务器（鉴权+限流+计量）→ 火山。<br>中转服务只需转发 WS 二进制帧，几乎无逻辑，一台小机器能扛很多 |
| 折中 | 服务端下发**短期临时凭证**，客户端直连火山 —— 但火山 sauc 目前是长期 Key 鉴权，没有 STS 式临时票据，此路不通 |

> **建议**：MVP 阶段做「用户自带 Key（BYOK）」存 Keychain —— 既零成本又零合规风险，还能直接发布开源。商业化时再加中转层。

---

## 6. 形态 B（IMK 输入法）的可行性分析 ⚠️

这是本方案**唯一有实质不确定性**的部分，必须单独立项验证。

**核心问题**：`IMKInputController` 的设计假设是「用户按键 → 输入法处理 → 更新预编辑区」。
而语音输入是**没有按键事件**的 —— 上屏必须由**音频回调（异步、非 UI 线程）**驱动。

**好消息：核心可行性已有证据支撑**

1. **`client()` 可以在 `handle(event:)` 之外被合法调用** —— `IMKInputController` 的 `candidateSelected:` 由**候选窗鼠标点选**触发，SDK 头文件注释明确写「典型响应是调用 client 的 `insertText`」。这就是「非键盘事件驱动上屏」在 IMK 设计里成立的直接依据。`updateComposition` 也是主动刷新 marked text 的入口。
2. **商业先例已存在** —— 搜狗输入法 Mac 版把语音输入内置在输入法里，双击 Option 触发、支持「按着说」、宣称「所说即所见」实时上屏。

**但有三条硬约束必须遵守**：

| # | 约束 | 后果 |
|---|---|---|
| 1 | ⭐ **所有 `client()` 调用必须在 MainActor，且 `setMarkedText` / `insertText` 是唯二不能异步派发的方法** | WebSocket 回调必须 hop 到主线程后**同步**调用，不能再嵌套异步，否则顺序错乱 |
| 2 | **输入法强烈建议开 App Sandbox**，且必须带 `com.apple.security.temporary-exception.mach-register.global-name` = `$(PRODUCT_BUNDLE_IDENTIFIER)_Connection`，`Info.plist` 的 `InputMethodConnectionName` **必须等于同一字符串**（这是唯一合法值，不是「建议」） | 不一致会导致沙盒下 `NSConnection` 建立失败，用户切过去直接不工作 |
| 3 | **不能长期强持有 `client` 对象** | `deactivateServer` 之后再用会崩。用 weak 或每次 `client()` 现取 |

> 注：「必须开沙盒」的说法过强 —— 沙盒是强烈推荐 + 上架 App Store 的必要条件，但**不是输入法能跑起来的硬性前提**，旧的未沙盒输入法仍可工作。

**真正的未知项是第 3 件事**：

| # | 待验证 | 风险 |
|---|---|---|
| ⚠️ | **输入法进程能否申请并拿到麦克风权限？** 输入法是无 UI 的后台 agent，麦克风 TCC 弹窗的归属与展示时机很别扭 | **高** |

**因此推荐的 IMK 架构是双进程**（不要等 Spike 失败再改）：

```
┌────────────────────────────┐         ┌──────────────────────────┐
│  主 App（有 UI，不沙盒）     │  XPC /  │  IME 助手（开沙盒）        │
│  · 麦克风采集               │  本地   │  · 只做 setMarkedText     │
│  · 火山 WebSocket           │ socket  │  · 只做 insertText        │
│  · 权限引导 / 设置 UI       │◄───────►│  · 装在 ~/Library/        │
│                            │         │    Input Methods/        │
└────────────────────────────┘         └──────────────────────────┘
     ⚠️ 两个进程沙盒策略相反，签名/打包必须分开处理
```

**Spike 验证方案（1~2 天）**：
1. 建最小 IMK 输入法（参考 `vChewing-macOS` 工程结构 + Shiki Suen 2026 指南），装到 `~/Library/Input Methods`
2. 在 `IMKInputController` 里起 `Timer`，每 300 ms 主动 `setMarkedText` 递增文本（**注意在 MainActor 同步调用**）
3. 在 备忘录 / VSCode / iTerm2 / Chrome 观察渲染与替换
4. 在输入法进程里调 `AVAudioEngine`，看能否拿到麦克风权限

> ⚠️ **还有一个产品级风险，工程解决不了**：中文用户必须**放弃搜狗/微信/鼠须管的拼音方案**才能切到你的输入法。
> 而 `TISSelectInputSource`（程序化切换输入源）对 CJKV 复杂输入源有已知 Carbon bug，沙盒 App 下还会出现「图标切了但实际没切」—— **「热键时临时切到自家输入法、结束再切回」这条路不可靠。**

---

## 7. 权限清单（macOS）

| 权限 | 何时需要 | 静默查询 API | 关键坑 |
|---|---|---|---|
| **麦克风** | 必需 | `AVCaptureDevice.authorizationStatus(for: .audio)` | `Info.plist` 需 `NSMicrophoneUsageDescription`；⚠️ **硬化运行时/沙盒下还必须有 `com.apple.security.device.audio-input` entitlement，否则系统根本不弹授权框** |
| **辅助功能** | `.defaultTap` 吞事件、AX 读写、CGEvent 注入 | `AXIsProcessTrustedWithOptions([kAXTrustedCheckOptionPrompt: true])` | 不会自动弹窗，需手动添加；授权后通常要重启 App |
| **输入监控** | `.listenOnly` 的 CGEventTap | `IOHIDCheckAccess(kIOHIDRequestTypeListenEvent)` → granted/denied/unknown；`IOHIDRequestAccess` 主动弹窗 | `Info.plist` 需 `NSInputMonitoringUsageDescription` |
| **自动化 (Apple Events)** | AppleScript 兜底注入（`System Events`） | — | ⚠️ **这是独立于辅助功能的第二个 TCC 弹窗**，首次控制 System Events 时弹 |

> ⭐ **权限分野的关键规则**：`CGEventTapCreate` 的 `options` 决定要哪个权限 ——
> `.listenOnly` → **输入监控**（`kTCCServiceListenEvent`）；`.defaultTap`（可修改/吞事件）→ **辅助功能**（`kTCCServiceAccessibility`）。
> **已有辅助功能权限时不会再弹输入监控。** 所以如果你已经要辅助功能（注入必需），直接用 `.defaultTap` 可以少要一个权限。

**⚠️ 分发方式已被权限锁死**：

> 辅助功能相关 API **与 App Sandbox 不兼容** → 主 App 不能沙盒 → **上不了 Mac App Store**（已有剪贴板工具因使用 `CGEvent.post` 被 Guideline 2.4.5 拒审）。
> **只能 Developer ID 签名 + 公证 + DMG/Sparkle 自更新。**
> 而 IME 助手又必须沙盒 —— 两个进程策略相反，签名打包要分开处理。

**⚠️ TCC 会莫名失效**（两个不同成因，都表现为「授权还在但功能不工作」）：

1. **绑定代码签名身份** —— 重新签名会产生「新身份」，需要重新评估或重新授权。**开发迭代期每次重签都可能要去系统设置里移除再重新添加。**
2. **系统升级重置** —— macOS 小版本升级后辅助功能授权被重置，且后台可执行文件常常不出现在权限列表里。

> → 必须运行时**周期性轮询** `AXIsProcessTrusted()`，失效时在菜单栏图标上显示明确异常态，文案写「请先**移除**再重新添加」。

**权限引导设计（安装转化率的成败点）**：
```
首次启动 → 引导页，三项分别检测 + 每 1~2s 轮询自动前进
  ① 麦克风      [授权]      ← AVCaptureDevice，点击直接弹系统窗
  ② 辅助功能    [打开设置]   ← x-apple.systempreferences:...?Privacy_Accessibility
  ③ 输入监控    [打开设置]   ← 若已有辅助功能则可跳过
  → 授权后无法自动生效的项，明确提示"需重启 App"
```

> 💡 **降级方案**：提供「仅剪贴板模式」—— 只需麦克风权限，识别完自动复制，用户自己按 ⌘V。
> 这能把权限流失的用户接住（Wispr Flow 用户明确抱怨「要授权好几次很烦」）。

---

## 8. 延迟优化清单（按收益排序）

| 优化 | 收益 | 难度 | 风险 |
|---|---|---|---|
| ⭐⭐ **环形音频预缓冲**（麦克风常驻空转，热键按下时把之前 300~500 ms 的 PCM 一并送出） | 消除冷启动 + **彻底消除首字丢失** | 低 | **无** —— 纯本地、无网络、无额外计费。**首选** |
| ⭐ WebSocket **建连与采集并行**，前 200 ms 音频缓冲后补发 | 消除 100~300 ms 握手延迟 | 低 | 低 |
| ⚠️ WebSocket **提前预热挂着** | 同上 | 中 | **高** —— 会撞 `45000081` 等包超时；长空闲的 `URLSession` socket 首次 write 会失败需用全新 session 重试。若做必须配 ≤8s 回收窗口 |
| ⭐ `end_window_size` 调档 | 尾字延迟大幅可调 | 极低 | 产品决策，见 [05 章](05-产品设计方案.md) §5.4 |
| 首包用 100 ms、后续 200 ms | 首字延迟 -100 ms | 低 | 低 |
| 关闭 gzip（PCM 压缩率本来就低） | 省 CPU，延迟微降 | 极低 | 无 |
| `enable_accelerate_text` | 首字更快 | 极低 | **牺牲准确率**，慎用 |
| 上屏用剪贴板而非逐字模拟 | 长文本上屏 -数百 ms | 低 | 剪贴板副作用 |

> **推荐组合：环形预缓冲（兜底首字）+ 建连与采集并行（省握手）**，不做长时间挂连预热。

**目标指标**：首字上屏 < 800 ms，整句上屏 < 1.2 s。

> ⚠️ **这两个数字目前是估算，没有一手实测。** M0 阶段必须实测四个分段并写进验收标准：
> ① 热键按下 → 首包音频发出　② 首包 → 首个 partial 返回　③ partial → definite 到达　④ definite → 文本落进目标 App

---

## 9. 测试方案

### 9.1 ⭐ 立项前的 go/no-go 冒烟测试（3 天，不可跳过）

> **在写任何产品代码之前**，先验证「非 IME 路线的产品前提是否成立」。

**测试矩阵**：5 个基准 App × 3 种注入方式

| | 备忘录 | Chrome 输入框 | VS Code | 微信 | iTerm2 |
|---|---|---|---|---|---|
| AX 直写 | | | | | |
| 剪贴板 + ⌘V | | | | | |
| CGEvent unicode | | | | | |

每格记录：成功率、失败模式、耗时。

> **判据（硬门槛）**：
> **如果剪贴板路径在这 5 个里有 2 个及以上失败 → 非 IME 路线的产品前提不成立，必须立刻回到 IMK 重新评估。**

⚠️ **AX 的成败一律靠「写后回读 `kAXValue`/长度」校验，不能信返回码** —— 它大量场景返回 `kAXErrorSuccess` 但 UI 没变。

### 9.2 常规测试

| 层级 | 测试内容 |
|---|---|
| **协议层** | 用固定的 16k PCM wav 回放推流，断言识别文本、`definite` 出现位置、错误码处理。**不依赖麦克风，可进 CI** |
| **音频层** | 各采样率设备、蓝牙切换、热插拔、长录（5 分钟不松手）、内存与句柄泄漏 |
| **上屏层** | 扩展 9.1 的矩阵到 15+ App，每个跑「短文本/长文本/含 emoji/含换行/快速连续上屏」 |
| **热键层** | ⭐ **长稳测试**：运行 8 小时后热键是否还灵（验证 tap 自动重启逻辑）；重签名后是否还灵（验证 TCC 引导） |
| **端到端** | 标准语音集（数字、英文混读、专有名词、方言）跑 CER；记录首字/尾字延迟分布 |
| **异常** | 断网、切 Wi-Fi、Key 失效、服务端 5xx、麦克风被占用、切换前台 App、**Secure Input 开启**（在 iTerm2 里开 Secure Keyboard Entry 测） |

---

## 10. 目录结构建议

```
语音输入/
├── docs/                          # 本调研文档
├── spike-python/                  # M0：协议验证（Python，一次性）
│   ├── sauc_client.py
│   └── test_with_wav.py
├── VoiceCore/                     # Swift Package，纯逻辑，可测试
│   ├── Audio/          AudioCapture.swift, Resampler.swift
│   ├── VAD/            SileroVAD.swift
│   ├── ASR/            ASRClient.swift, DoubaoStreamingASR.swift, Protocol.swift
│   ├── Text/           TextStabilizer.swift, PostProcessor.swift
│   └── Inject/         TextInjector.swift, Keystroke/Clipboard/AX
├── VoiceApp/                      # 形态 A：菜单栏 App
│   ├── MenuBarController.swift
│   ├── FloatingHUD.swift
│   ├── HotkeyManager.swift
│   └── Settings/
├── VoiceIME/                      # 形态 B：输入法（阶段二）
│   └── InputController.swift
└── Tests/
```

> `VoiceCore` 独立成 Swift Package 是关键 —— 它让形态 A 和形态 B 共享 90% 的代码，也让协议层可以脱离 UI 做单元测试。
