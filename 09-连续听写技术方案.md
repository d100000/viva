# 09 · 连续听写技术方案 —— 一直说、一直写

> 目标：用户连续说话不停顿时，文字也能**一段一段**写进光标处，而不是全部憋到松手才一次性上屏；并且想说多久说多久，中途不被服务端掐断。

---

## 1. 为什么现在做不到「一直说、一直写」

### 1.1 根因：上屏的唯一触发器是服务端「判停」，而判停的唯一依据是**静音**

现在的数据流（`VoiceSession.handle` → `injectSentence`）：

```
音频 → 豆包服务端 VAD 判停 → utterance.definite=true → newDefinite → 写进光标处
                └── 判停条件：静音 ≥ end_window_size（当前 600ms）
```

- **只有 `definite` 会写进目标 App**。partial（未定稿文本）只进悬浮条 —— 这是刻意设计：partial 会被服务端**反复修订**（后文出现才能纠正前文的同音字），而本项目硬性不变量是「只追加，绝不退格」，写进去的字收不回来。
- `definite` 的产生**只有一条路**：静音超过 `end_window_size`。文档 02 §12.2 的实测结论「end_window_size 必须小于说话的自然停顿」说的就是它。
- 所以：**你一直说、不留 ≥600ms 的停顿 → 服务端永远不判停 → 永远没有 definite → partial 无限增长、悬浮条里文字一直变，但一个字都不会写进输入框** —— 直到松手发末包，服务端强制定稿，才全部涌出来。

这正是「中间识别不会断掉」的完整解释：不是 bug，是判停机制在无停顿语流下的必然行为。

### 1.2 次要限制（长时间连续说话时才暴露）

| 问题 | 现状 | 后果 |
|---|---|---|
| 服务端会强制收流 | `handleFinished` 里已观察到「VAD 强制收流 / 最大时长 / asyncFinal」，当前处理是**报错**：「本次识别被服务端结束了，请松开热键重新说」 | 说得越久越可能被掐断，后半段要重按重说 |
| `result_type: "full"` 全量返回 | 每一帧都带全部历史文本 | 连续说 10 分钟后每帧几 KB 起，带宽/解析开销线性涨（上限 8MB 消息） |
| 服务端参数救不了 | `vad_segment_duration`（语义切句）**配置了 end_window_size 后失效**，且它同样以静音为条件；协议里没有「按时长强制出 definite」的参数 | 调参数没有出路，必须客户端做 |
| 按住不放的人体工学 | 长文口述按着右⌘十分钟不现实 | 「一直说」需要配套的点按开关模式（文档 07 待办 #12） |

---

## 2. 三条候选路线

### 路线 A：调服务端参数 —— ❌ 走不通

`end_window_size` 已是 600ms（安全区内），再小也需要**有静音**才触发。协议没有「无静音强制分段」参数（§1.2）。放弃。

### 路线 B：客户端「稳定前缀提交」—— ✅ 核心方案

**观察**：partial 的修订几乎全部发生在**尾部**（声学上下文只影响邻近字）；带标点（`enable_punc` 已开）的 partial 里，**逗号/句号之前的文本一旦稳定几帧，之后被修订的概率极低** —— 标点本身就是语言模型认为「这个小句已闭合」的信号。

**规则**（全部条件同时满足才提交）：

1. 当前未定稿 utterance 的 partial 长度 ≥ `minLength`（默认 20 字）—— 短句走原有 definite 流程，不来抢
2. 找 partial 中**最后一个标点**（，。？！；：、）的位置 P
3. P 之前的前缀在**连续 N 帧**（默认 3 帧）里逐字相同 —— 帧间稳定性
4. P 距离 partial 尾部 ≥ `tailGuard`（默认 8 字）—— 尾部安全边距，修订区不碰
5. → 把前缀 [0, P] 写进光标处，记录「本 utterance 已注入的字素数」

**definite 到达时的对账**：该 utterance 定稿后，只注入 `definite 文本去掉已注入字素数之后的尾巴`。若定稿版本在已注入区间内有修订 —— 接受不一致（不变量：绝不退格），靠上面 4 个条件把概率压到可忽略。

### 路线 C：会话轮转（配合 B，解决「说不断」）—— ✅ 必须做

单一 WebSocket 会话说到底有服务端最大时长。把「被动被掐」变成「主动轮转」：

```
触发器（按优先级）：
 T1  服务端主动收流（asyncFinal 但用户还按着）→ 立即自动轮转，不再报错
 T2  会话年龄 > rotateAfter（默认 50s）且刚收到一个 definite → 借这个干净边界轮转
 T3  会话年龄 > hardRotate（默认 75s）且一直没有 definite → 在最近 300ms 音量谷值处强制轮转
```

轮转过程（对用户完全无感）：

```
旧会话 finish()（末包）─→ 尾巴文本按对账规则注入
      ↘ 同时：新会话 start()，握手期间音频写入本地续接缓冲
新会话 streaming ─→ 先 flush 续接缓冲，再实时喂
```

- 文档 02 §10.8 的告诫是「**包级** VAD 门控污染流式 cache」；轮转是**会话级**边界 + 新会话从零开始，恰好是文档推荐的正确粒度。
- T3 的中断词风险（一个词被劈在两个会话）用「音量谷值选刀口」压低；T1/T2 天然是干净边界。
- 计费不变：按累计音频时长，轮转不多花钱。

---

## 3. 实施设计

### 3.1 新增配置（`Config.swift`）

```swift
/// 连续听写：partial 稳定前缀逐段上屏（润色开启时自动失效，见 3.4）
var progressiveCommit: Bool = true
var progressiveMinLength: Int = 20     // partial 达到多少字才启用
var progressiveStableFrames: Int = 3   // 前缀连续稳定帧数
var progressiveTailGuard: Int = 8      // 距 partial 尾部的安全边距（字素）
/// 会话轮转
var rotateAfterSeconds: Int = 50       // 有 definite 边界就轮转的年龄阈值
var hardRotateSeconds: Int = 75        // 无 definite 也强制轮转的上限
```

### 3.2 新文件 `PartialCommitter.swift`（纯逻辑，可单测）

```swift
struct PartialCommitter {
    mutating func feed(partial: String) -> String   // 返回本帧可安全提交的增量（无则空）
    mutating func reconcile(definite: String) -> String  // 定稿对账，返回还需注入的尾巴
    mutating func reset()
    private(set) var committedGraphemes: Int
}
```

- 全部按**字素簇**计数（emoji/组合字符安全），与 `injectedCharCount` 同口径
- 稳定性判断存最近 N 帧前缀哈希，不存全文 N 份
- **不含任何 UI / 注入逻辑** —— 用合成 partial 序列做单元测试（含「尾部修订」「标点后回改」「utterance 切换」用例）

### 3.3 `DoubaoStreamingASR` 改动（小）

- `ASRUpdate` 增加 `activeUtterance: String`（当前未定稿 utterance 的文本，不再和其它拼接混用）
- 暴露 `sessionAge`（start 起算）；其余不动 —— 轮转由 VoiceSession 层做，ASR 客户端保持一次性生命周期（现有的 didFinish/cursor 语义完全不变，这是踩过数据竞争坑的稳定代码，不重构）

### 3.4 `VoiceSession` 改动（核心）

- 持有 `PartialCommitter`；`handle(_:)` 里在 `deferCommit == false` 且 `config.progressiveCommit` 时喂 partial，产出即 `injectSentence`
- `newDefinite` 到达时改走 `reconcile` 注入尾巴（原路径是全量注入）
- 新增 `rotate()`：`asr` 换新实例的过程中，`capture.onChunk` 的音频进 `rotationBuffer`；新会话 streaming 后 flush。**committedText / PartialCommitter / periodHold 跨轮转保留**
- `handleFinished` 里 `interrupted == true` 且用户仍按着（`state == .listening`）→ 不再报错，改调 `rotate()`（T1）
- 定时器驱动 T2/T3；T3 找刀口用 `AudioCapture` 已有的 level 回调近 300ms 滑窗最小值
- **润色互斥**：`deferCommit == true`（润色开/试听/commitOnlyAtEnd）时 progressive 整个旁路 —— 润色语义就是「拿全文重写」，与逐段上屏天然冲突，UI 上在设置页写明

### 3.5 点按开关模式（配套，可后置）

双击右⌘ = 锁定连续听写，再按一下 = 结束。`HotkeyManager` 已有按下/松开时序，加双击窗口判定即可。长文口述不按住键，这条不做「一直说」就只是理论能力。

---

## 4. 风险与对策

| 风险 | 概率 | 对策 |
|---|---|---|
| 已注入前缀被 definite 修订 → 屏幕文字与最终识别不一致 | 低（4 条件把关） | 阈值可调；极端在意的用户关 `progressiveCommit`；历史记录存的是 definite 全文，可对照 |
| T3 强制轮转劈断词 | 低 | 音量谷值选刀口；75s 内说话没有任何标点停顿的语流本身罕见 |
| 轮转握手期音频堆积（网络差） | 中 | rotationBuffer 上限 10s，超限丢弃最旧并提示；握手失败回退为「继续旧会话直到服务端掐断」 |
| 新会话首包后热词/参数遗漏 | — | 轮转复用 `begin()` 同一套 `sendFullClientRequest`，无第二套代码路径 |
| PartialCommitter 和 PeriodHold 叠加出错 | 中 | 逐段提交走既有 `injectSentence`（内含 PeriodHold），不开新注入路径；单测覆盖组合场景 |

## 5. 测试方案

1. **单元测试**（新增，纯逻辑）：PartialCommitter 喂录制好的真实 partial 序列（先加 `DOUBAO_DUMP_FRAMES=1` 把一次真实会话的全部帧存下来做 fixture）
2. **selftest 扩展**：`--selftest` 支持拼接多段音频模拟 90s 连续语流，验证 T2/T3 轮转与文本无缝拼接（协议层全链路，不需要权限）
3. **真机验收**（需要用户）：对着念一篇文章 3 分钟不刻意停顿 —— 验收标准：① 每 5~15s 有一段写进编辑器 ② 全程无重复/丢段 ③ 与历史记录里的全文 diff ≤ 2 字/百字
4. **计费对照**：轮转前后 billedSeconds 与账单核对（文档 02 §10.13 的遗留待办顺手做掉）

## 6. 分期

| 期 | 内容 | 交付判据 |
|---|---|---|
| P1 | PartialCommitter + VoiceSession 接入 + 配置项 | 单测全绿；真机连续说 60s 内有逐段上屏 |
| P2 | 会话轮转 T1/T2/T3 | selftest 90s 连续语流无缝；服务端掐断不再报错 |
| P3 | 点按开关模式 + 设置页文案/开关 | 双击锁定可用 |

> 预估：P1 与 P2 各一个独立 commit，互不依赖可分开验证；P3 最小。
