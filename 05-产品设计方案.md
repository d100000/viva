# 产品设计方案

> ⚠️ **本章在深度竞品调研后做过一次重大修正**。初版的三个差异化支点（真流式 / 中文最强 / 为 AI 工作流而生）**有两个已经站不住**——见 §3。

---

## 1. 必须先接受的三个残酷事实

在谈定位之前，先把调研查到的现实摆清楚，否则会做出一个「一上线就被碾压」的产品。

### 事实一：字节自己已经下场，而且做的就是你想做的

**豆包输入法 macOS 版 2026-05-12 正式上线**（v0.9.0，包体 159~180 MB，免费无广告）。它已经实现了：

- ✅ **真·逐字流式上屏**，不限语音时长
- ✅ 长按说话 + **免按输入（免提常开）** 两种模式
- ✅ **ASR 侧内建**自动标点、语气词/语病/口误修正、**学习个人改词习惯**（一次修正长期生效）
- ✅ 轻声识别、强抗噪、中英混说自动切换
- ✅ 与你同源的豆包语音模型

> **也就是说：「接豆包 API 做个流式听写」这件事，字节官方已经免费做完了，而且做得比你能做的更深（它能改 ASR 链路，你只能调 API 参数）。**

它的**结构性约束**（也就是你唯一的缝隙）：

| 约束 | 为什么是机会 |
|---|---|
| **必须登录豆包账号**，否则 AI 功能不可用 | 有人不愿意登录 |
| **是系统输入法**，要在「系统设置 → 键盘 → 输入法」里添加 | **占掉输入法槽位，用户被迫在「豆包」和「自己的拼音方案」之间二选一** |
| 需要辅助功能权限**读取当前 App 名与窗口标题** | 隐私敏感用户会介意 |
| 实测缺陷：**按下激活键有延迟，漏掉开头几个字** | ⭐ 可攻击点 |
| 实测缺陷：嘈杂环境中英混说英文仍会错（Claude → Cloud） | ⭐ 可攻击点（热词） |
| 强依赖稳定网络 | — |
| **Windows 版尚未发布** | 时间窗口 |

### 事实二：开源赛道已被占位，且有两个项目正好压在你的规划上

| 项目 | 它已经占了什么 |
|---|---|
| **[type4me](https://github.com/joewongjc/type4me)**（1366★ / MIT / macOS 14+） | 火山流式 + **13 家 LLM 后处理** + 自定义 prompt（`{text}`/`{selected}`/`{clipboard}` 变量）+ 热词与片段替换 + **全本地历史 CSV**。作者自我定位「Typeless 平替」 |
| **[typeflux](https://github.com/mylxsw/typeflux)**（322★ / Swift） | ⚠️ **菜单栏形态（非 IME）+ 实时流式 + 本地模型可离线不上云 + 自称「桌面端语音 Agent」** —— 正好是我原本规划的「非 IME 形态」和「隐私自持」两个差异点 |

闭源侧还有 **LazyTyper**（免费，12 个模型含豆包，Win/Mac/Linux）和**闪电说**（端侧优先，配合豆包流式 2.0）。

### 事实三：「LLM 润色」已经是标配，不是差异化

Wispr Command Mode、Aqua Edit Mode、superwhisper Modes（GPT-5 / Claude Haiku 4.5 / Llama 4 / Grok 4.1 / Gemini 3.0 Flash）、VoiceInk Enhancement、Typeless 自动排版、Spokenly Agent Mode、type4me 13 家 LLM —— **人手一份**。

真正的能力分层是：

| 层 | 能力 | 现状 |
|---|---|---|
| L1 | 标点 / 去口水词 | **豆包 ASR 已内建，免费送** |
| L2 | 按 App 场景化格式 | 竞品普遍已有 |
| L3 | **选中文本 + 语音指令就地改写** | 海外已验证（Aqua Edit Mode / Wispr Command Mode），**中文侧基本空白** ⭐ |
| L4 | Agent 化（口述 → 执行动作） | 前沿 |

---

## 2. 修正后的产品定位

> **一句话**：给「已经有自己输入法、且要大量口述长文本」的 Mac 用户，做一个**不占输入法槽、不强制登录、不上传任何东西**的语音口述工具。

**不是什么**：
- ❌ 不做输入法（豆包/微信/讯飞的战场，且逼用户放弃拼音方案）
- ❌ 不做「又一个 Whisper 套壳」
- ❌ 不做通用听写（打不过免费的豆包输入法）

**是什么**：
- ✅ **AI 编程场景的口述工具** —— 给 Claude Code / Cursor 说需求
- ✅ **可编程热词引擎** —— 输入质量的杠杆
- ✅ **零信任隐私姿态** —— 音频只发往用户自己的火山账号

---

## 3. 差异化：哪些成立，哪些已作废

| 初版支点 | 判定 | 理由 |
|---|---|---|
| ~~① 真流式~~ | ❌ **作废** | 豆包输入法、微信输入法、type4me、typeflux、Aqua Voice 全都做了 |
| ~~② 中文最强~~ | ❌ **作废** | 用的是同一个豆包模型，官方输入法还能改 ASR 链路 |
| ③ 为 AI 工作流而生 | ✅ **成立但需具体化** | 不能停留在口号，要落到终端/IDE 注入可靠性 + 代码术语热词 |

**修正后的四个支点**（都必须是对手结构上做不到或不愿做的）：

### ① 形态错位：不占输入法槽 ⭐⭐⭐

豆包/微信/讯飞都是**系统输入法**。V2EX 上大量用户在纠结「macOS 输入法选豆包还是微信」——**本质就是被迫二选一，要放弃鼠须管/自然码/系统拼音**。

后台常驻 + 全局热键 + 注入，用户可以**继续用自己的拼音方案**。这是社区已经明确表达的真实需求。

> ⚠️ 但 typeflux 已经占了这个形态。**必须叠加下面三点才够。**

### ② 首字不丢 ⭐⭐

**豆包官方 Mac 版实测存在「按下激活键漏掉开头几个字」的缺陷。**

解法是**环形音频预缓冲**：麦克风常驻空转，热键按下时把**之前 300~500 ms 的 PCM 一并送出**。成本极低，感知极强。

> 可以直接当营销点：**「不吃掉你的前三个字」**。

### ③ 可编程热词 ⭐⭐⭐

这是**豆包流式 API 相对本地 Whisper 系工具的独家优势**，而官方输入法作为通用产品不会专门优化。

按**前台 App / 当前 Git 仓库 / 剪贴板内容**动态生成 `corpus.context`：

```
在 Cursor 里且当前仓库是 my-project
  → 自动扫描仓库里的标识符、依赖名、目录名
  → 生成热词：Claude、Anthropic、WebSocket、sauc、bigmodel_async、AVAudioEngine…
  → 直接解决「Claude 被识别成 Cloud」这个最高频抱怨
```

### ④ 隐私自持 ⭐⭐

这个品类出现过因「上下文感知」功能上传窗口截图而引发的信任争议，社区反应强烈。
（⚠️ 相关报道均来自竞品/SEO 博客等二手来源，未找到一手材料，此处只作为**品类风险**记录，不对具体厂商的行为下结论。）

明确写进首屏：
> 音频只发往**你自己的火山账号**，不经过任何第三方服务器；历史与配置全本地；**不截屏、不读前台内容**。

---

## 4. 目标用户（必须收窄）

| 用户 | 场景 | 为什么选你而不是免费的豆包输入法 |
|---|---|---|
| ⭐⭐⭐ **AI 编程重度用户** | 给 Claude Code / Cursor 口述需求和长 prompt | 代码术语热词 + 终端/IDE 注入可靠 + 英文标识符不转拼音 + 不占输入法槽 |
| ⭐⭐ **有固定输入法方案的人** | 全场景 | 不用放弃鼠须管/自然码 |
| ⭐⭐ **隐私敏感的专业用户** | 律师、投资、医疗 | 不登录账号、不截屏、BYO key |
| ⭐ 有 RSI/手腕伤的人 | 全场景 | 刚需，付费意愿最高 |

> **不要做「所有 Mac 用户」**。那是豆包输入法的地盘，免费且更好。

---

## 5. 交互设计

### 5.1 热键：⚠️ 默认绝对不要用 Fn

**Fn 键已经是三方争抢的战场**：微信输入法 Mac 2.0.0 抢占全局 Fn 作语音热键，豆包输入法默认也是 Fn。V2EX 已有连续吐槽帖（「微信劫持 Mac 全局 Fn 键」「继微信之后，微信输入法也开始劫持 fn 键」）。

再加上工程问题：Fn 短按被系统占用（切输入法/表情/听写）；**大量第三方键盘根本不上报 Fn 的 `flagsChanged`**。

> **决策：默认用右 Command（keyCode 54）**，热键完全可配置，**首次启动时检测与微信/豆包输入法的冲突并提示**。
> 这本身就是可宣传的体验点。

### 5.2 三种模式

| 模式 | 触发 | 适用 |
|---|---|---|
| ⭐ **按住说话**（默认） | 按住右 ⌘ | 短句。零误触发、成本可控 |
| **点按开关** | 双击右 ⌘ | 长文口述 |
| ~~免提常开~~ | — | ⚠️ **暂不做**。按小时计费 + 常开 = 挂机 8 小时 8 元/天。且豆包输入法免费提供 |

### 5.3 双态渲染（核心交互）

```
        ┌─────────────────────────────────────┐
        │ ▂▄▆█▆▄▂  我要去北京出差，下周三…     │  ← 紧贴光标
        └─────────────────────────────────────┘
                   已定稿(实心)  未定稿(灰/斜体)
```

- **未定稿（`definite:false`）**：灰字/斜体，只在 HUD 里显示，**不进输入框**
- **已定稿（`definite:true`）**：注入目标 App

> ⚠️ **「流式上屏」和「LLM 润色」天生冲突**：流式吐的是未定稿文本，套 LLM 会让屏幕上的字反复跳变重写。
> **必须两段式**：流式期间只出原始 ASR（保实时感）→ definite 或整段结束后再用 `enable_nonstream` 二遍识别 / 轻量 LLM 原地精修（保成稿质量）。**绝不在流式过程中反复调 LLM。**

### 5.4 上屏节奏可调（产品级决策，不是参数微调）

`end_window_size` 决定 `definite` 何时到达，**直接决定上屏节奏**：

| 档位 | `end_window_size` | 效果 |
|---|---|---|
| **快嘴模式** | 300~500 ms | 短语级上屏，最像「边说边打字」；代价是断句碎、标点质量下降 |
| **思考模式** | 1500~3000 ms | 成句完整、标点准；代价是上屏慢 |
| 默认 | 800 ms | 官方默认。**对「边想边说」的口述场景偏激进，会把句子切碎** |

> 做成用户可选档位，用人话包装参数。这是能调出来的体验优势。

### 5.5 必须做对的细节

1. **首字不能丢** → 环形预缓冲回溯 300~500 ms（差异化支点②）
2. **Secure Input 检测** → 密码框、iTerm2 的 Secure Keyboard Entry 下注入全失效且无报错。必须主动调 `IsSecureEventInputEnabled()` 并提示「已复制到剪贴板，请按 ⌘V」
3. **热键健康指示** → CGEventTap 会被系统静默禁用，把静默失败变成可见失败
4. **紧急取消** → Esc 丢弃本次
5. **锁定目标 App** → 录音期间切走则取消并提示
6. **用量与费用实时显示** → BYOK 模式下是刚需，也是差异化

---

## 6. 功能清单

### P0 — MVP

- [ ] 右 ⌘ 按住说话（热键可配置，检测 Fn 冲突）
- [ ] 豆包流式 ASR（`bigmodel_async`）
- [ ] 双态渲染 HUD（灰字预览 + definite 注入）
- [ ] 三级注入策略链 + 兼容性矩阵（见 [04 章](04-技术实现方案.md)）
- [ ] **环形音频预缓冲**（首字不丢）
- [ ] Secure Input 检测与兜底
- [ ] 权限引导 + 热键健康检测
- [ ] BYO Key（Keychain）
- [ ] 用量统计与费用预估

### P1

- [ ] ⭐ **可编程热词**（前台 App / Git 仓库感知）
- [ ] 替换词典
- [ ] 快嘴/思考模式档位
- [ ] 两段式 LLM 精修
- [ ] 历史记录 + 「粘贴上一段」全局快捷键
- [ ] 点按开关模式

### P2

- [ ] ⭐ **L3 语音指令编辑**（选中文本 + 说出修改要求就地重写）—— 中文侧空白
- [ ] IMK 输入法形态（真增量上屏）
- [ ] Windows 版（豆包输入法尚未发布，有时间窗口）
- [ ] 本地后备后端

---

## 7. 定价：订阅在中文市场几乎不可行

### 锚点已经被拉到地板

| 产品 | 价格 |
|---|---|
| 豆包 / 微信 / 讯飞输入法 | **免费** |
| LazyTyper | **永久免费** |
| Spokenly 本地 + BYOK | **永久免费不限量** |
| type4me / typeflux | **开源免费** |
| VoiceInk | **买断 $25 / $39 / $49**（1/2/3 台） |
| MacWhisper | €59 买断 |
| Typeless | 免费 8000 字/周；Pro 年付 $12/月、月付 $30/月 |
| Aqua Voice | 免费 1000 词；Pro $8/月 |
| superwhisper | Pro $8.49/月；终身约 $849（已涨价） |
| Wispr Flow | 免费 2000 词/周；Pro $15/月、年付 $12/月 |
| Willow | Pro $15/月、年付 $12/月 |

社区对订阅制的抵触很具体：有开发者公开批评「月收 $20 却只是包了一层 Whisper API，实际 10 分钟 API 用量只值 $0.06」。

### 建议：一次性买断 + BYO Key

> **对标 VoiceInk（$25~49 买断），不要对标 Wispr（$15/月）。**

成本账**直接算给用户看**（这本身就是卖点）：

| 用户 | 每天说 | 月成本 |
|---|---|---|
| 中度 | 30 分钟 | **约 15 元** |
| 重度 | 2 小时 | **约 60 元** |

> ⚠️ **但 BYO Key 有一个必须拍板的矛盾**：BYO key 要求用户自行注册火山引擎、开通语音服务、管理 AK/SK —— 这是**开发者行为，不是大众消费者行为**。
> 所以目标用户就必须收窄到「愿意自己配 key 的开发者 / 重度口述者」（正好就是 §4 的第一类人）。**二者必须一致，不能含糊。**

---

## 8. 中文用户的真实痛点排序（与海外不同）

V2EX 社区反馈，前三名**都不是准确率**：

| 排序 | 痛点 | 对产品的启示 |
|---|---|---|
| 1 | **公共场合不敢说**（「整个办公室都在语音输入跟菜市场一样」） | 语音输入天然受场景限制，**TAM 比想象的小**。可考虑耳语识别/低音量优化 |
| 2 | **保密性为零 / 联网监控** | 隐私姿态是刚需不是加分项 |
| 3 | **「说错一点就得重来」** | ⭐ 直指 **L3 语音指令编辑**的价值 |
| 4 | 准确率、价格 | 热词兜底 |
| 5 | 嗓子耐久度 | — |

---

## 9. 关键体验指标

| 指标 | 目标 | 备注 |
|---|---|---|
| 首字上屏延迟 | < 800 ms | ⚠️ **目前全文所有延迟数字都是估算，无一手实测** |
| 整句上屏延迟 | < 1.2 s | 同上 |
| **首字丢失率** | **0%** | 豆包官方 Mac 版在这里有缺陷，是可攻击点 |
| 上屏成功率 | > 99%（五个基准 App） | 见 [06 章](06-实现路径与里程碑.md)的 go/no-go 门槛 |
| 常驻内存 | < 150 MB | Electron 竞品被喷 800MB / 8% idle CPU，原生 Swift 是口碑位 |
| 最低系统版本 | ⚠️ **待拍板** | 豆包/讯飞支持 10.15+ 且 Intel/ARM 双架构；卡在 14+ 或 Apple-Silicon-only 等于把老 Intel 用户整块让出 |

---

## 10. 风险与应对

| 风险 | 应对 |
|---|---|
| ⭐ **被豆包输入法正面碾压** | 立项前先装豆包输入法实测，量化首字延迟/掉字率/中英混说，写进方案作基线。**如果你规划的差异点它已做到三个以上，应重新评估立项** |
| ⭐ **typeflux 已占住「非 IME + 隐私自持」** | 必须叠加「可编程热词 + 首字不丢 + AI 编程场景」才够。持续监控其 release |
| **TAM 比想象的小**（公共场合不敢说） | 主打居家/独立办公场景；不要按「所有 Mac 用户」估算市场 |
| 注入兼容性 | 见 [04 章](04-技术实现方案.md)，剪贴板兜底 + 按 bundleId 的兼容性矩阵 |
| 权限流失 | 精简引导；提供「仅剪贴板模式」（只需麦克风权限） |
| 火山接口变更/涨价 | `ASRClient` 做成 protocol，可切换后端 |
| 许可证污染 | VoiceInk 是 GPL-3.0，**只能参考设计不能抄代码**；type4me / Handy / openless 均 MIT 可安全取用 |

---

## 11. 竞品监控清单

持续跟踪：豆包输入法 Mac 更新节奏与 **Windows 版发布**、[type4me](https://github.com/joewongjc/type4me) release、[typeflux](https://github.com/mylxsw/typeflux) release、LazyTyper、闪电说、Typeless、Aqua Voice 的 Streaming/Edit Mode 迭代。
