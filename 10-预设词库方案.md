# 10 · 预设词库方案

> 2026-07 调研。目标:让 Viva 开箱即对「AI 工具/编程/职场」高频专名有一流识别率,
> 且词库可**远程更新**(改仓库 JSON → 全网用户一天内生效,不发版)。

---

## 1. 调研:竞品怎么做词库

| 产品 | 机制 | 关键设计 |
|---|---|---|
| **Wispr Flow** | Dictionary = **词汇(提概率)+ 替换规则(确定纠正)** 双机制 | 从用户修改里自动学词(过滤常见词只学专名);CSV 批量导入;云同步 |
| **superwhisper** | Custom Vocabulary + Text Replacements,同样双机制 | 文档明示两者分工 |
| **Aqua Voice** | 行话调优模型 + 自定义词典 | Pro 档 **800 词上限** —— 行业给词典的量级参考 |
| **VoiceInk** | Custom dictionary + replacements | 开源,本地 |
| **讯飞输入法** | 行业词库泛化 + **支持导入 txt 词库** | 高频人群:移动办公/客服/长文本口述 |
| **豆包输入法** | 个人改词记忆(改一次长期生效) | 即我们的 ReplaceRule 对标来源 |

**结论:双机制是行业共识,Viva 的架构(hotwords + replaceRules)方向正确。**
差异在容量:竞品词典几百上千条,我们的直传热词受**豆包 100 tokens(约 60 词)硬上限**约束
—— 所以「大而全」必须放在本地替换规则(无上限、零成本),热词只放「最容易识错的」。

## 2. 调研:用户在哪些方向高频用语音输入词库

按「专名密度 × 语音输入使用频率」排序:

1. **AI 工具与模型名** —— 变化最快(模型名 churn 以月计),误识率最高
   (Claude→克劳德/Cloth、DeepSeek→迪普西克),是 2026 语音输入的第一大痛点。
   2026 新热词:MCP、vibe coding、智能体、具身智能、推理模型、Harness。
2. **编程与基础设施** —— Viva 目标用户(开发者)的日常:框架/语言/中间件专名。
3. **互联网职场** —— 协作工具名(飞书/Notion/Figma/Jira)+ 黑话缩写(OKR/PRD/DAU)。
4. 垂直行业(医疗/法律/金融)—— 讯飞的方向,离我们用户远,**不做**。

## 3. 调研:GitHub 现成词库(选词来源,不直接搬)

| 仓库 | 内容 | 对我们的用处 |
|---|---|---|
| [jiqizhixin/AITD](https://github.com/jiqizhixin/Artificial-Intelligence-Terminology-Database) | 机器之心 AI 术语库,2442 条中英对照 | AI 词**选词参考**。⚠️ CC BY-NC-SA(非商用),只参考不拷贝 |
| [LiangWei88/WebDevDictCN](https://github.com/LiangWei88/WebDevDictCN) | 前端开发词库 1000+ 条(搜狗格式) | 编程词选词参考 |
| [thunlp/THUOCL](https://github.com/thunlp/THUOCL) | 清华中文词库,含 IT 类 + 词频 DF | 按词频筛高频 IT 词 |
| [iDvel/rime-ice](https://github.com/iDvel/rime-ice) | 雾凇拼音,长期维护简体词库 | 通用词参考 |
| [studyzy/imewlconverter](https://github.com/studyzy/imewlconverter) | 深蓝词库转换(格式互转标准工具) | 将来支持「导入搜狗/Rime 词库」时用 |

**为什么不整库导入**:输入法词库是「拼音→词」映射、动辄上万条,而 ASR 直传热词只有
60 词窗口;拿百科全书塞钥匙孔没有意义。**精选混淆度最高的,才是热词的正确用法。**

## 4. 方案

### 4.1 选词三原则

1. **只收 ASR 容易识错的**。纯中文常见词(微调/幻觉/多模态)豆包本来就识得对,
   不浪费 token;英文专名、中英混合词才是热词的价值区。
2. **热词排序即优先级**。合并窗口 60 词,用户词 > ai > it > work,先到先得 ——
   每个库内部按混淆度从高到低排。
3. **替换规则宁漏勿错**。只收「正常说话中几乎不可能是本意」的高特异性串
   (克劳德→Claude、库伯内提斯→Kubernetes);单字、常用词一律不收,学错一条毁所有。

### 4.2 词库矩阵(wordlists/*.json)

| 库 | 定位 | 默认 | 量级 |
|---|---|---|---|
| `ai.json` | AI 工具/模型/术语(churn 快,更新最勤) | ✅ 开 | ~36 热词 + 替换 |
| `it.json` | 编程语言/框架/基础设施(v2,AI 词移出) | ✅ 开 | ~36 热词 + 替换 |
| `work.json` | 互联网职场:协作工具 + 黑话缩写 | ❌ 关(按需开) | ~30 热词 |

### 4.3 分发链路(已实现,见 WordlistStore)

```
仓库 wordlists/*.json(version 字段) → GitHub raw → App 每 24h 拉取
→ version 更大才落 ~/.config/viva/data/wordlists/ → 内置副本兜底离线
```

维护流程 = 改 JSON、version+1、git push。**一天内全网生效。**

### 4.4 后续路线(按优先级)

1. **自学习平台热词表**(`boosting_table_name`):火山控制台可放几千词,
   突破 100 tokens —— 引导重度用户自建,Viva 提供表名填写入口。
2. **词库导入**:支持粘贴一列词(已有批量编辑)→ 将来支持搜狗/Rime 格式
   (用深蓝转换的解析逻辑)。
3. **改词记忆自动沉淀**:高频被用户手动改的错词,匿名统计后反哺预设替换表
   (需用户明确 opt-in,隐私红线)。

## 5. 数据来源

- Wispr Flow Dictionary 文档 · superwhisper Vocabulary 文档 · VoiceInk 对比页
- TechCrunch《The best AI dictation apps》2026-05
- AI.com.tw《2026 AI 术语 200 条》/《2026 关键字 TOP15》· 新华网《2026:智能体爆发年》
- GitHub:AITD / WebDevDictCN / THUOCL / rime-ice
